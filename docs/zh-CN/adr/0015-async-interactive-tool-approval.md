# ADR 0015 — 异步交互式工具审批

**简体中文** · [English](../../en/adr/0015-async-interactive-tool-approval.md)

- 状态：已接受
- 日期：2026-07-17

## 背景

`PermissionManager` 已经提供确定性的 `allow`、`ask` 和 `deny` 策略判定，并保证显式
deny 优先。第一版 TUI 没有安全暂停工具调用并等待用户响应的机制，因此交互组合有意
把未解决的审批转换为拒绝。

审批 UI 不能成为代理运行时的依赖，用户响应不能覆盖策略拒绝；同时必须证明具有副作用
的工具在等待审批、被拒绝或审批等待被取消后都不会启动。

## 决策

- `PermissionManager` 继续作为同步策略引擎；`PermissionApprover` 是独立、可选的异步
  端口。无头组合不提供审批器，保留原有的失败关闭行为。
- 策略结果为 `ask` 时，运行时先产生 `tool_approval_requested`，等待审批器，然后产生
  `tool_approval_resolved`。只有允许结果之后才能出现 `tool_started`。处理器缺失时拒绝；
  取消等待会终止本轮、按 ADR 0016 记录配对的错误结果，并且绝不启动工具。
- TUI 通过 `SessionApprovalBroker` 和模态框提供精确操作结果，以及仅由运行时生成的
  有类型范围候选。拒绝仍是初始焦点；`Esc`、`Ctrl+C` 和 `D` 也会拒绝。用户可以为
  普通的 `search_replace`/`apply_patch` 主工作区目标授予工作区编辑范围，也可以为保守
  识别出的 `test`、`static_check` 或 `git_read` 命令族授权。不存在“全部允许”选项，
  模态框内的 `Ctrl+C` 只拒绝本次请求，不取消整轮。
- 精确操作的会话批准仍使用“规范工具名 + 完整参数映射”的 SHA-256 摘要。宽范围批准
  是独立的有类型 grant，同时绑定可信 session identity 与工作区规范根路径。它只能在
  规范文件系统计划或保守 Bash 分类完成后由运行时生成，模型不能伪造候选。每次调用仍
  先经过 `PermissionManager`，因此显式 deny、显式 ask、模式决定和高风险操作都不能
  被此前的宽范围批准绕过。无法规范化为 JSON 或无法安全分类的 Bash 命令都会降级为仅
  允许本次。
- 模态框接收有界、由策略层生成的摘要，而不是通用原始参数映射。Bash 会显示有界命令，
  因为命令本身就是待授权操作；搜索替换只显示工作区路径和操作数量，隐藏 old/new 文本；
  patch 内容同样隐藏。
- 审批请求与结果追加到现有会话事件审计日志；不会改变数据库 schema，也不会创建持久
  权限规则。

## 后果

交互式编辑和命令现在可以安全暂停并等待决定，测试也能观察到批准之前工作区保持不变。
精确操作的会话缓存可以减少重复询问，同时不会授权整个工具或命令类别。

批准不会跨进程重启保留，目前也不能创建经过审查的持久 allow/deny 规则。代理会在首个
决定后重新检查排队的等价请求，但仅允许一次、拒绝或取消绝不会替等待请求授权。丰富的
参数差异、用户自定义拒绝反馈和 ACP 范围选项展示仍属于后续纵向切片；ACP 继续只提供
现有的精确操作选项。

## 验证

审批队列、ACP 边界和交互界面由 Neuro Code 自身的契约与行为测试验证。
