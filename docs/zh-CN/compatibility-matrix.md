# 兼容性矩阵

**简体中文** · [English](../en/compatibility-matrix.md)

状态取值：`unassessed`（未评估）、`planned`（已规划）、`partial`（部分实现）、`compatible`（兼容）或 `intentionally-different`（有意不同）。“证据”指向固定版本的 Rust 源项目。

当前 Windows sandbox production 源码验证为 CI run `32374860136`，head 为
`1175fa046cde263a9a1c9afbb1a32b7f6ed34914`；23 个 job 全部完成并通过。最终分支更新仅
增加证据文档。

| 能力 | 目标 | 状态 | 证据 / 备注 |
|---|---|---|---|
| Semantic text accents | TUI | intentionally-different | 13 个主题分别映射 Markdown、语法高亮、工具与 diff 的语义色；仅渲染与静态检查，未运行测试套件。 |
| Settings visual hierarchy | TUI | intentionally-different | 宽屏默认外观分类；分组底色、条目分隔与表单间距。仅静态检查和渲染，未运行测试套件。 |
| Scoped interactive preferences | TUI | intentionally-different | 新增八项上下文、输入、提醒与后台限制；17 项偏好均支持用户/工作区范围与启动快照查看。只做静态检查与界面渲染，未运行测试套件。 |
| Persistent TUI agent settings | TUI | intentionally-different | 新增九项可保存并于下次 TUI 启动生效的执行、模型请求、工具与验证偏好；支持恢复继承与显式 CLI 参数优先。未运行测试套件，仅进行静态检查和界面渲染。 |
| TUI settings navigation | TUI | intentionally-different | 五组设置导航、跨分类搜索、当前值与生效说明、窄屏单列布局；沿用已有存储与权限边界。本轮仅渲染界面，未运行测试套件。 |
| Ultracode parent-workspace freshness projection | M5 | compatible | VF-4b 从 durable target lifecycle 推导 `ResultAdoptionRecord.parent_workspace_changed`，不增加 schema column。Target 若在 final-verification 的精确 post-apply `INDETERMINATE` transition 前到达 `APPLIED`，该事实仍被保留；pre-apply 不确定以及 no-target 结果保持 false，若其他 target 已到达 `APPLIED`，partial conflict/indeterminate 结果则保留该事实。一个稳定 adoption identity 在 recovery re-entry 中仍只对应未来一次 parent verification-mutation boundary。Worker verification evidence 不导入。详见 [ADR 0156](adr/0156-ultracode-parent-workspace-freshness.md) |
| Ultracode objective-size boundary | M5 | compatible | 唯一的 domain-owned `MAX_SWARM_OBJECTIVE_BYTES` contract 为按 UTF-8 字节计算的 4 KiB。Agent Swarm request validation 与 Ultracode marker policy 共用它；超过边界且包含 marker 的 prompt 会在 durable branch claim 之前选择 `MAIN_MAX`。这是决策前 routing，不是 fallback，恢复不会重新分类已有的 durable decision。详见 [ADR 0141](adr/0141-automatic-ultracode-delegation.md) |
| Ultracode MAIN_MAX verification snapshot | M5 | compatible | 结构化 verification 同时支持 `MAIN_MAX` 与 `BOUNDED_SWARM` parent path。新的 request 缺少声明时由 `NormalTurnRequirementsPolicy` 只解析一次；显式非空或空 snapshot 原样冻结到 `TurnInput` 和不可变 Ultracode execution identity。Schema 30 增加两个 nullable snapshot column；NULL/NULL 保持 Legacy 模式，partial、损坏、超限或 fingerprint 不匹配的结构化 row fail closed。恢复只重放精确已提交 parent result，不重新调用 Provider 或 Finalizer。结构化 `BOUNDED_SWARM` 先执行 lower Swarm 与 adoption，再使用精确 snapshot 调用普通 parent runtime；Legacy NULL row 继续使用 external-result path。详见 [ADR 0155](adr/0155-ultracode-main-max-verification-snapshot.md) 与 [ADR 0157](adr/0157-ultracode-bounded-swarm-parent-verification.md) |
| 软件包与 CLI 组合根 | M1 | partial | 普通软件包已包含 Textual，并发布等价的 `neuro` 和 `neuro-code` 控制台脚本；`neuro code` 是显式 TUI 别名，三者都把启动目录作为工作区，全局工具安装后无需每次激活虚拟环境。CLI、TUI 和 ACP 共享与界面无关的 `ApplicationComposition`；独立原生安装程序仍待实现 |
| 预览包/版本边界 | M1 | partial | `src/neuro_code/_version.py` 是 `neuro_code.__version__`、Hatchling metadata、CLI 输出、inspection 和 ACP negotiation 共用的唯一版本来源。R1 使用 PEP 440 `0.1.0a1`；`scripts/release_candidate.py` 构建并验证 wheel/sdist、干净安装、内容安全、密钥安全和 SHA-256 provenance，但不执行公开发布。 |
| 生效配置检查 | M1 | partial | `neuro-code inspect`；已实现隐藏密钥的 Python 视图 |
| Provider service catalog 与 preset ownership | M3/P3A/P3B | partial | `ProviderServiceDescriptor` / `ProviderServiceCatalog` 描述当前十三个服务，包含 Ark、Qianfan、Bailian 与 TokenHub，保留现有 UI key 与 profile matching，并由可注入的 TUI Catalog 消费。Service metadata 不拥有 credential 或 runtime 执行；未来增加服务只需 descriptor 加测试。P3B 将 service、publisher、protocol、model、endpoint 及 billing/workspace metadata 分开。见 [ADR 0117](adr/0117-provider-service-capability-routing-foundation.md)、[ADR 0122](adr/0122-china-direct-provider-compatibility-expansion.md) 与 [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| 规范 model 与 hosted-tool capability | M3/P3A/P3B | partial | `ModelCapabilitySet` 提供 supported/unsupported/unknown 三态。Runtime 保留 service → protocol → model 上游事实，与可信 adapter implementation capability 求交，并只应用显式 configuration restriction；metadata 不能把 adapter 的 unsupported 或 unknown 提升为支持。P3B 增加 model-specific Chat/Responses/Anthropic 矩阵、可选 publisher hint，以及 TokenHub native/compatibility-converted/unsupported Responses metadata，但不增加 hosted vendor tools。现有 xAI builtin tool 保持不变；标准 OpenAI Responses 只有在显式配置 `builtin_tools = ["web_search"]` 时才暴露 hosted Web Search。Gemini Interactions 增加按 protocol/model 区分的 Google Search、URL Context 与 `MIXED_HOSTED_AND_CLIENT_TOOLS` fact；unknown Gemini model 仍失败关闭。Kimi、GLM、MiniMax、Ark、Qianfan、Bailian 与 TokenHub hosted web 保持 unknown 或 unsupported，除非另有可信实现。见 [ADR 0117](adr/0117-provider-service-capability-routing-foundation.md)、[ADR 0118](adr/0118-hosted-web-search-execution-and-sidecar-routing.md)、[ADR 0120](adr/0120-gemini-interactions-and-hosted-web-tools.md)、[ADR 0122](adr/0122-china-direct-provider-compatibility-expansion.md) 与 [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| Runtime role 与 model route | M3/M5 | partial | `RuntimeRole.MAIN` 与可选 `WEB_SEARCH` route 已完成校验、脱敏和独立执行；旧 `[routing] default`/`fallbacks` projection 为 MAIN，role-specific fallback 保持隔离。`WebSearchMode` 对 disabled/auto/inline/sidecar 做失败关闭解析，`WebFetchMode` 对 disabled/auto/local/inline 做无 sidecar 解析：hosted fetch 只有 MAIN 明确支持时保留，local fetch 只有 mode 允许后才注册。见 [ADR 0118](adr/0118-hosted-web-search-execution-and-sidecar-routing.md) 与 [ADR 0121](adr/0121-local-safe-web-fetch.md) |
| Hosted Web Search 与 Web Fetch 纵向切片 | P1/P1.2/P2/P3A | partial | 已实现有界的 canonical request/source/citation/result contract、`WebSearchService`、独立 WEB_SEARCH fallback、标准 OpenAI Responses 与 xAI sidecar、Anthropic Messages `web_search_20260318`/`web_fetch_20260318` server tool，以及 Gemini Interactions Google Search/URL Context hosted capability。Gemini 使用 stable stateless Interactions request、typed step lifecycle、有界 native replay、结构化 `url_citation` 提取、强制 Google Search sidecar 与 capability-aware inline/sidecar routing；DeepSeek/OpenAI-compatible MAIN → 本地 `web_search` → search route → canonical `ToolResult` 路径保持不变。Kimi、GLM 与 MiniMax MAIN 复用这条本地 sidecar 路径和 P2 local fetch；本轮不声称三家 native hosted web。Portable 测试覆盖 source bound、取消、usage 隔离、脱敏、不可信 evidence 边界以及三种 China MAIN composition。Gemini/China live smoke 需要显式开关和凭据。见 [ADR 0118](adr/0118-hosted-web-search-execution-and-sidecar-routing.md)、[ADR 0119](adr/0119-anthropic-hosted-web-search-and-fetch.md)、[ADR 0120](adr/0120-gemini-interactions-and-hosted-web-tools.md)、[ADR 0121](adr/0121-local-safe-web-fetch.md) 与 [ADR 0122](adr/0122-china-direct-provider-compatibility-expansion.md) |
| Local Safe Web Fetch | P2 | partial | 已实现 `WebFetchRequest`/`WebFetchResult`、`WebFetchService`、带 pinned A/AAAA validation 的 public-only `aiohttp` transport、手工有界 redirect、HTTPS downgrade rejection、direct/no-proxy/no-cookie GET、有界解压 body、text MIME allowlist、charset fallback、非执行 HTML-to-Markdown extraction、URL secret fail-closed、带 `[UNTRUSTED WEB CONTENT]` 的脱敏 tool result、provenance、cancellation 与 mode-gated composition ownership。Browser/JS/Playwright/PDF/crawling/auth/cookie/local-network/file/FTP/WebSocket/cache 与新 Provider 仍不支持。见 [ADR 0121](adr/0121-local-safe-web-fetch.md) |
| Provider model discovery strategy | M3/P3A/P3B | partial | Catalog discovery 支持显式 OpenAI-compatible、Anthropic、Gemini、static 或 manual-only strategy，并保留按 protocol 推导的兼容回退。Kimi、MiniMax、Qianfan OpenAI-compatible、Bailian OpenAI-compatible 与 TokenHub 使用有界 inference model path；GLM 与 Ark 使用有界官方/static evidence。远程可用性不会提升 model capability 或 protocol status。仍允许手工填写 model；TUI 已使用不含 credential、原子写入且有界的持久化 catalog cache，并在限定网络错误下回退到新鲜缓存。见 [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md)
| 命名供应商 profile 与选择 | M2/M3/P3A/P3B | partial | 已实现四种显式线路协议、可解析为具体协议的自动/推荐 UI 选择、默认/单次选择、旧 TOML 兼容、脱敏的 `providers list/inspect`、首次启动 TUI 设置、分类优先的设置流程、多 profile 新建/编辑/保存并使用/二次确认删除、显式 OpenAI Responses/兼容 Chat/DeepSeek/Kimi/GLM/MiniMax/Ark/Qianfan/Bailian/TokenHub 预设、service → endpoint variant → protocol → model 选择、model-specific protocol validation、由用户触发的连接测试与有界内存远程模型选择器及手动回退，以及通过 `/provider`、`/model` 和 Ctrl+P 选择并持久保存受管默认项。平台 credential 与 normal settings 分离，并复用 environment/direct/explicit proxy policy。P3B 路由全部共享 `OpenAICompatibleProvider`、`OpenAIResponsesProvider` 或 `AnthropicProvider`，没有四套平台 runtime adapter。endpoint/service identity 参与 native context affinity，包括同 publisher route。平台 hosted tool 仍未实现；provider-neutral Web Search 与 P2 local safe Web Fetch 保持不变。见 [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| 类型化 Provider 投影接缝 | M3 | compatible | `ProviderOption` 与 `ProviderSelectionResult` 由 `neuro_code.application.providers.contracts` 拥有；Provider 应用服务、bootstrap 和 TUI 使用规范接缝，而 profile/session 与旧 runtime 导入保留保持 identity 的兼容 re-export。Provider 生命周期与选择行为保持不变 |
| CC Switch 兼容 | M2 | partial | 已实现只读映射 `NEURO_CODE_CC_SWITCH_CONFIG`、三种 backend 格式、回环 `PROXY_MANAGED`、环境变量引用、内联密钥拒绝和项目配置覆盖；仍不包含 CC Switch 数据库/进程控制及其内部故障转移 |
| 安全供应商故障转移 | M2 | partial | 已实现有序按需备用 profile、第一个事件提交边界、单次运行单向选择、可审计失败/选择事件、`--no-failover`、汇总错误和不透明会话来源保护；新增输出前有界重试、冷却熔断与不含密钥的进程内健康快照。跨进程持久化健康状态仍明确不实现
| Provider 类型化失败分类 | M2 | partial | `ProviderFailure` 事实对象对认证、授权、限流、无效请求、模型、上下文、服务端、超时、网络、协议和未知失败进行分类，并提供有界脱敏 detail、状态码、phase、Provider/模型身份、有界 `Retry-After` 和证据来源。通用 HTTP 边界采取保守策略：404 不直接断言模型不存在，没有结构化依据的 429 不自动重试，413 不直接断言上下文溢出。五组协议专属离线 fixtures 覆盖有文档依据的 OpenAI-compatible/Responses、Anthropic、Gemini Generate Content 和 Gemini Interactions envelope；Anthropic `billing_error` 仍是 authorization，`rate_limit_error` 是 rate limit，Gemini `RESOURCE_EXHAUSTED` 是覆盖 RPM/TPM/RPD/spend 维度的文档 429 rate-limit 事实。`ProviderFailurePolicy` 独立处理 retry、circuit 和输出前 failover；local runtime 失败不会故障转移，不计入熔断的事实不会污染瞬态 health，输出提交后禁止重放。本项不宣称完整 Provider 兼容性，也未运行 live/付费验证。详见 [ADR 0126](adr/0126-provider-typed-failure-taxonomy.md) |
| 供应商 HTTP 代理策略 | M2/M3 | partial | 已实现按 profile 配置环境/直连/显式环境变量模式、TUI 编辑与保存前校验、受管默认项启动失败的 TUI 内恢复、四适配器统一 HTTPX 选项、检查/错误脱敏、含义不明 SOCKS scheme 的严格诊断，以及 `socks` 可选安装 extra；保留无效继承 `ALL_PROXY` 时，显式路由已通过 DeepSeek 手动验证。PAC、多代理挂载和默认捆绑 SOCKS 支持仍待实现 |
| 无头单次提示 | M2 | partial | 已实现流式文本/工具循环，以及纯文本、JSON、JSONL 输出 |
| 回合工作区 Checkpoint 与 Undo | M2 | partial | B1 通过类型化 `SourceWorkspaceCheckpointGrant` 与现有 checkpoint service，保护普通用户回合中最新一次 eligible mutation 的 Git-visible source-checkout projection。权限批准后、第一次有界 mutation 执行前持久化一个 checkpoint，同回合后续 eligible mutation 复用；ignored/unbounded/unsupported mutation 会在执行前使保证失效。`/undo` 与 `sessions undo <SESSION_ID>` 仅在 idle 时执行，使用仅保留最新的 `AVAILABLE`/`UNAVAILABLE`/`ROLLED_BACK` 状态，支持重启 proof、精确 index/file restore，并通过现有 verification generation 传递一次 mutation。Managed-worktree proof 不变；ignored file、工作区外副作用、非 Git workspace、进程监控、历史 rollback、ACP/model undo 以及整个文件系统恢复仍不支持。详见 [ADR 0158](adr/0158-turn-workspace-checkpoint-undo.md) |
| 只读 Git 变更检查 | M2 | partial | B2 为 repository identity、HEAD、branch/detached 状态、upstream 计数、porcelain-v2 status metadata 以及分离的 staged（`HEAD -> index`）和 unstaged（`index -> working tree`）diff 提供一个有界类型化 projection。`git_inspect` 与 CLI `inspect git` 共享 application service 和加固的固定命令 adapter；output 有界并脱敏，binary 与 untracked 状态仅提供 metadata，malformed 或不一致 snapshot fail closed。不修改 workspace/index/ref/config/history/checkpoint/session/verification，不增加 schema、TUI/ACP surface、automatic untracked-content discovery、recursive submodule inspection 或 commit/rollback workflow。详见 [ADR 0159](adr/0159-read-only-git-change-inspection.md) |
| 持久历史可寻址性与有界回填 | CM1 | compatible | `SessionItemQueryService` 从 canonical ordered durable item sequence 派生有界的 `list`/`search`/`read` projection。不透明且绑定会话的 `shr1_` reference 会校验 ordinal 与 item fingerprint；`session_history` 是只读且由 Runtime binding 当前会话的 model tool，不接受任意 session ID。隐藏 reasoning/native/synthetic/internal 内容与 mutation 继续排除。不增加第二份 history store、Working Set、跨会话浏览或新的 CLI/TUI/ACP surface；CM3a rollover 按需复用这个既有 history source。详见 [ADR 0160](adr/0160-durable-history-addressability-and-rehydration.md) |
| 结构化会话 Working Set | CM2 | compatible | `WorkingSetSnapshot` 是由六个固定 section 组成的有界持久任务状态 projection，使用不可变类型化 entry、可选 CM1 source reference、明确的 model/history provenance、redaction 和 revision-CAS 替换。一个 SQLite row 可在重启后恢复；没有 row 的 legacy session 解析为空，格式错误的 row 失败关闭，fork/subagent session 不继承状态。Runtime 每次刷新非持久 synthetic projection，并将它追加在 conversation 之后作为 volatile tail；`session_working_set` 只能读取或更新 Runtime binding 的 session，不能选择或浏览其他 session。Working Set 不等于 canonical history、compaction summary 或跨会话 memory。详见 [ADR 0161](adr/0161-structured-session-working-set.md) |
| Fresh active-context rollover | CM3a | partial | Normal Agent 暴露 exclusive、无参数、runtime-bound 的 `new_context` control。持久单调递增的 `context_generation` marker 与 exclusive `context_generation_start_index` boundary（schema 33）保持同一个 session/history，并让 recovery 从 system prefix、current user input、刷新后的 instructions/skills、刷新的 Project Memory snapshot、current Working Set tail 以及之后的 runtime/model/tool item 重建 fresh active projection。旧的 canonical item 仍可通过 CM1 获得；synthetic rollover context 不持久化。Provider affinity/failover、permission、verification、workspace state、finalization 和现有 safe-boundary compaction 保持不变；child/subagent binding 不接收该 control；automatic policy 由 [ADR 0163](adr/0163-automatic-fresh-context-rollover.md) 单独规定。详见 [ADR 0162](adr/0162-fresh-context-rollover.md) |
| Automatic fresh-context rollover | CM3b | partial | Normal finalizing Agent 继续以 `ContextPreflight` 为权威：`SAFE` 正常继续，`UNKNOWN` 保持现有行为，不自动 compaction 或 rollover；不可约的 `BLOCKED` 在 compaction/rollover 之前直接停止。可约的 `COMPACTION_REQUIRED` 最多使用一次已有 safe-boundary automatic compaction；rebuild 和再次 preflight 后，使用包含 exact current user、rebuild 后 Working Set/instructions、冻结 tools、provider identity、output reserve 和 safety margin 的完全相同 fresh projection 做 preview。只有第一次真实 provider request 之前的 `SAFE` preview 才能 commit 一次使用 CM3a durable contract 的 automatic rollover；`BLOCKED`/`UNKNOWN` preview 不 mutation、不调用 provider，mid-turn pressure 也不能丢弃未提交的 tool/native evidence。随后 rebuild 并 defensive final preflight 一次，再进入 provider 或已有 deterministic budget block。同一个 session、canonical history、CM2 Working Set、CM1 按需恢复、provider-native boundary/affinity、failover origin、permission、workspace、verification、finalization 和 pending-turn recovery 语义保持不变；不增加 counter、compaction metadata、schema、threshold 或 automatic subagent policy。详见 [ADR 0163](adr/0163-automatic-fresh-context-rollover.md) |
| 面向模型的工具结果保护边界 | CM4a | partial | `ToolExecutor` 为 hooks、`ToolExecutionResult`、verification、supervision、workspace/progress evidence、artifact 和终态事件保留完整规范 `ToolResult`，然后只在面向模型的 `Role.TOOL` 消息边界应用一个 Provider 无关的 `ToolResultContextProjection`。在既有 `ToolContext.output_byte_limit` 以及 64-KiB/16,384 估算 token 全局上限内的结果原样透传；超大文本获得确定性 UTF-8 安全头尾投影和有界省略元数据。并行批次在 `min(64 KiB, output_byte_limit * call_count)` 内按调用确定性均分字节份额，并依据当前 preflight capacity 分配近似 token 份额。只有通过 Runtime 自有 store 边界返回的类型化 artifact 才能启用更完整 artifact marker；custom 工具的通用 metadata 不足以证明。既有不透明 output artifact 会复用但不向模型暴露 ID 或路径，也不创建重复 artifact。下一次请求继续使用正常 `ContextPreflight` 和 Provider window 计量。不增加 LLM 摘要、语义检索、结果重写、新 artifact store 或自动清除。详见 [ADR 0164](adr/0164-model-facing-tool-result-guard.md) |
| 确定性 Microcompaction V1（微压缩） | CM4b | partial | 只在普通用户 turn、请求容量已知且出现可执行的 `COMPACTION_REQUIRED` 压力时触发，并在 preflight 前或工具批次后一次批量处理。只替换完整、有序、位于历史 turn 且由 Runtime 确认成功的结果 group；最近三个历史 group、当前 turn、error、media/binary、格式错误组和终态未知的结果保持完整。固定 marker 只改模型可见 `ModelContext`；session history、artifact、audit、verification 和 recovery fact 均保持规范且可读取。应用层按 session/generation 固定精确 group fingerprint；只有稳定前缀/compaction boundary 变化，或追加至少八个稳定条目/2,048 个估算 token 后，才允许再次批处理。提案必须至少节省 1,024 个序列化条目字节和 256 个估算 token。最终 Context Preflight 会评估压缩后的请求：`SAFE` 跳过 Full Compaction 和 rollover；压力仍在时进入既有路径。不含正文的聚合 telemetry 使用既有 preflight event。状态有界且仅驻留内存；重启后终态未知的结果保持完整。详见 [ADR 0173](adr/0173-deterministic-microcompaction-v1.md) |
| 临时与已提交的最终响应契约 | M5 | partial | `neuro_code.application.runtime.final_response.FinalResponseContract` 仍是 provisional 与 committed 终态响应语义的唯一 owner。`AgentRunResult` 与 Runtime 的 `TURN_COMPLETED` projection 只接受 committed 响应，并携带有界的 response source、commit state、verification state 与工作区 generation metadata；不增加 schema 列。VF-2b 保留无工作区修改回合的普通流式行为，但对有修改、需要验证或已有证据的回合启用有界逐步骤文本 buffer；tool-step 文本仅作为中间输出释放，无工具 candidate 则由既有 evidence-aware finalizer 或 deterministic fallback 替换。Candidate 不进入 assistant 历史、重放或完成持久化，`MODEL_OUTPUT_STARTED` 仍是恢复标记。Provider、CLI/TUI/ACP 与 session schema 契约不变；验证发现、验证要求和 UltraCode 集成不属于本切片 |
| 结构化验证要求 | M5 | partial | `neuro_code.domain.execution.verification_requirements` 拥有有界不可变的验证要求声明、稳定的 `req-v1` 身份、可合并的 strength/provenance 与 snapshot fingerprint。`neuro_code.application.runtime.verification.VerificationTracker` 继续是唯一可变事实所有者：`covered_requirement_ids` 提供精确 evidence 关联，每个要求的最新事实不受诊断 ring 淘汰影响，复用全局 workspace generation freshness，类型化 blocker 产生有界的 `BLOCKED` evaluation。Required 失败为 `FAIL`；Required 缺失、过期或 blocked 为 `INCOMPLETE`；只有 advisory 的声明不会把完成表述为充分验证。VF-3b 会将同一个可选 snapshot 经由 `RunTurnRequest`、会话 binding、`AgentRuntime`、`AgentLoopRunner` 和 `TurnInput` retry/recovery JSON 传播，不增加 schema 列；缺少字段的 legacy 数据兼容，而存在但损坏的 snapshot 会失败关闭。VF-4c 将同一契约扩展到结构化 `BOUNDED_SWARM`，先执行 canonical Swarm/adoption path，再调用 parent AgentRuntime；worker 仍是本地执行且不接收 parent requirements。Requirement discovery/acquisition、专用 TestRunner、UI 修改与 generic verification integration 不属于这些切片 |
| 普通 Agent 验证获取边界 | M5 | partial | `NormalTurnRequirementsPolicy` 仅为没有显式 snapshot 的新普通用户回合，在 UltraCode 路由之后、TurnInput/Provider/工具执行之前准确生成一次稳定的 Required `ON_WORKSPACE_MUTATION` generic requirement。显式 snapshot、显式空 snapshot、legacy recovery、后台任务、子代理和 UltraCode 路径保持各自语义。`resolve_verification_coverage` 复用现有可信 Bash classifier，只把 exact generic requirement ID 关联到已识别的 `bash:test`/`bash:static_check` 命令；分类 scope 仍是描述性 metadata。已识别命令失败为 `FAILED`；本切片只有无歧义的被拒绝 `MODE` permission fact 可以产生 `POLICY_RESTRICTION` / `BLOCKED`。`EXPLICIT_RULE` denial 暂不分类，因为该 source 同时涵盖显式 deny rule、headless ASK-to-deny 和 restrictive Bash conversion；不会使用 reason-string inference。generic finalizer 文案保持保守。不增加自动发现、framework detection、专用 TestRunner、UI/schema 变更或 UltraCode verification integration。详见 [ADR 0154](adr/0154-normal-agent-verification-acquisition-boundary.md) |
| 应用自有思考强度 | M3 | partial | 已实现供应商中立的 `low`/`medium`/`high`/`xhigh`/`max`/`ultracode`、默认 `high`、请求指引、`ModelContext` 传递、`--effort`、TUI 控件及偏好持久化。`max` 仍是普通单智能体深度。显式 Kimi K3 与 GLM 5.3/5.2 dialect 会把普通主路径的 `max` 投影映射到配置的原生 `max` 字段；其他 dialect 省略原生强度字段但保留应用层指引。显式 `ultracode` 回合进入 durable 有界 delegation service，由它准确选择一个 `MAIN_MAX` 或既有 `BOUNDED_SWARM` 路径。详见 [ADR 0141](adr/0141-automatic-ultracode-delegation.md) |
| 交互工作模式 | M3/M4 | partial | 已实现应用自有 `normal`/`accept-edits`/`plan`/`auto`、`Shift+Tab` 循环、`/mode MODE` 补全、常驻显示、原子偏好持久化、profile/会话重新应用、仅请求生效的模型指引和确定性权限模式映射。计划模式现在具有有界、供应商中立的 `update_plan` 工具、持久会话恢复/分叉继承、本地化的 TUI `/plan DESCRIPTION` 和 `/view-plan`/`/show-plan`，以及绑定到当前计划修订的本地 `/comment-plan STEP COMMENT` 反馈；显式 `/execute-plan`/`/run-plan` 交接会在规范执行请求之前记录持久事件，并在一条不透明会话任务生命周期中保存不可变计划修订快照，随后只切换到 `accept-edits`。已实现有界显式 `/schedule-plan`/`/queue-plan` 持久化和 `/run-task TASK_ID` 认领，每会话最多四个排队任务；排队任务不会自动启动或创建子代理。除非启动时显式授权 `--always-approve`，`auto` 会标为采用 accept-edits 默认值的安全预览；模型安全分类器仍待实现，显式规则和进程沙箱始终有效 |
| 轮次与工具计时反馈 | M3 | partial | 已实现每个模型步骤的单调时钟首个可行动结果耗时、整轮完成/失败耗时及本地/托管工具耗时。每次调用保留稳定 call-ID 状态，连续活动则显示为一个带有界聚合摘要的折叠组。单击或 Enter 打开固定高度、只含一个调用的 Inline Peek；再次单击已打开的 Peek 或按 Esc 会将其收起，焦点移动后也有效。上/下方向键选择调用，Enter 打开其 Output/Input/Meta Inspector。Inspector 会跟随实时生命周期更新而不阻塞 Conversation；运行计时器对每个 Summary 组最多刷新一次，并跳过已打开的 Peek/Inspector 布局。Conversation 永不加载 artifact 输出，Inspector 则通过既有属于当前会话、256 KiB 且已脱敏的读取边界懒加载并明确提示截断。Transcript Copy 不受 disclosure 状态影响。这些耗时是客户端观测，不是供应商私有推理遥测；持久化完整工具记录仍待实现 |
| 上下文窗口预算与用量显示 | M3 | partial | 已实现可选的 profile 正整数 `context_window_tokens`、脱敏选择/故障转移传播、启动/恢复估算、供应商报告的输入加输出用量事件、紧凑可本地化的状态/提示显示、`/status` 报告及 50%/80% 警示配色。该字段是本地元数据，不会作为 API 参数发送。C1 在容量和 `max_output_tokens` 已知时为普通 Agent 增加保守预检：估算逻辑上下文、准确工具定义、请求 metadata、输出保留值及确定性的 5% 余量（下限 128 token，上限 2,048 token）。CM4b 会在可执行的上下文压力下先进行一次固定缓存快照的确定性微压缩，再对最终模型可见投影执行 preflight；若为 `SAFE`，则跳过 Full Compaction；压力仍存在时，既有安全点 Full Compaction 最多可在首次请求前执行一次。仍超容量的请求会在 Provider 调用前停止。未知容量或输出 metadata 仍为 `UNKNOWN` 并保持既有 Provider 路径；精确的供应商 tokenizer 仍不可用 |
| OpenAI 兼容/xAI Chat 端点 | M2/P3A | partial | 已实现 Chat Completions 流式响应、有界 `max_tokens`、工具、经过校验的原生用户图片块、思考模式工具往返连续性，以及失败关闭的上游导入可见上下文回放；DeepSeek V4 DSML 解析仅由显式 `openai-chat` `dialect = "deepseek-v4"` profile 启用。Kimi、GLM 与 MiniMax 复用同一 adapter，并通过显式 dialect mapping 处理当前 thinking/reasoning control、tool-choice 限制、MiniMax `max_completion_tokens`/`reasoning_split`、结构化 MiniMax reasoning replay、model discovery 与 usage/cache 字段。已有有界 model fixture 与 opt-in live test；full native structured-output parity 与 hosted web 仍不声明 |
| OpenAI Responses/xAI 方言 | M2 | partial | 已实现通用 Responses 子集、xAI 扩展、原生回放、SSE 生命周期、错误脱敏、显式 hosted-search 选择及 source/citation 提取；标准 OpenAI `web_search` 必须显式启用。有状态 response ID 仍未实现；Provider 中立的有界持久化压缩记录和安全点自动压缩已实现
| Anthropic 提供商 | M2 | partial | 已实现原生 Messages SSE、文本/推理、工具、用量、错误、密钥脱敏、经过校验的用户/工具结果图片块、保守的 hosted Web Search/Web Fetch capability gating、版本化 direct-caller server tool、lifecycle event、原生 continuation replay、有界 `pause_turn`、client/server 混合 tool 处理及结构化 source/citation 提取；在线/模型专用夹具仍需显式运行 |
| Gemini 提供商 | M2 | partial | Legacy `gemini-generate-content` 继续作为 compatibility adapter，保留 `streamGenerateContent` SSE、文本/思维、工具、用量、错误、thought-signature 往返和经过校验的用户内联/File API 图片项。新增 `gemini-interactions` 独占 stable `/v1/interactions` SSE、stateless `store=false` request、thought/function/builtin step、native affinity replay、Google Search、URL Context 与 mixed hosted/client capability gate；在线/模型专用夹具需显式开启 |
| 核心工具注册表与模式 | M2 | partial | 已实现独立 Python 契约，以及按能力启用的后台 Bash/任务输出/多任务等待/终止 schema |
| 读取/列举/grep 工具 | M2 | partial | 已实现有序有界 `read_files`、拒绝链接并过滤生成目录的 `list_tree`、确定性多查询 `grep_many`；批量输出会脱敏并受字节限制，部分读取失败仍可用。独立只读调用可在有界并行组中运行；副作用和交互控制调用保持独占。内部实现由 canonical filesystem read、discovery 和 search 适配器分别拥有；公共 registry 与导入门面保持不变 |
| 搜索/替换编辑 | M2 | partial | 已实现原子精确替换与路径边界检查 |
| Bash 执行与取消 | M2/M3 | partial | 已实现有界前台输出、空标准输入、已配置供应商/代理凭据剥离、超时/取消清理、POSIX 进程组 TERM/KILL、失败关闭的 Windows Job Object 创建与原子 `PROC_THREAD_ATTRIBUTE_JOB_LIST` 加入/受限标准句柄继承/后代等待/整 Job 终止、会话作用域受管后台启动/快照/有界单任务与多任务事件等待/超时/幂等终止/绑定切换/应用关闭生命周期、带任务输出/多任务等待/终止去重的有界模型专用完成提醒、重启感知的有界会话唤醒账本，以及复用同一进程树的前台自动转后台；持久全局默认、供应商覆盖和会话级 `/auto-wake on|off` 优先级已实现；已通过可选状态目录存储为截断的本地 Bash 预览提供有界脱敏完整输出文件，显式审查后的密钥注入以及 ACP 客户端终端/文件读取器的产物支持仍待实现；明确的自动唤醒可注入每任务 2 KiB、每批 8 KiB 的脱敏输出预览 |
| 权限规则优先级 | M2/M3 | partial | 已实现 deny/ask/allow 优先级、逐片段检查、包装器剥离、嵌套 `bash -c`、复杂脚本失败关闭、异步单次允许/拒绝、内存会话批准、每 ACP session 的 `session/request_permission`、path/operation-aware 规则，以及有界原子 JSON 规则持久化。完整规则/文件访问语法与交互式规则编辑器仍待实现
| SQLite 会话事件存储 | M2/M3/M4/M5 | partial | 当前开发 schema v35，采用并发初始化器串行化的 v1→v35 migration，包含 profile 亲和与固定沙箱元数据、稳定标题、同步 FTS5、durable plan/task/turn recovery、background-wake 与 compaction ledger、context-generation 边界、result adoption、Working Set 以及 parent-child `subagent_links`。内部 migration 路径作为开发兼容性进行测试；Neuro Code 尚无既有公开发布版本，因此 preview-to-preview 升级兼容性要在首个公开预览版建立发布基线后才开始。既有 list/resume/fork/export 与 workspace-identity 行为保持有界；execution record、compaction item、subagent link 和 writable lease 不进入 FTS、导出/导入或分叉复制。 |
| 会话标题与全文搜索 | M3 | partial | 已实现首条可见用户提示的确定性十词标题、上游 `generated_title` 保留、通过 `sessions rename` 和 TUI `/rename`/`/title` 原子更新手动标题与 FTS、标题十倍权重的 SQLite FTS5、多词 AND 到 OR 回退、cwd 过滤、分页、命中字段、可选摘要、`sessions search` 和 TUI `/sessions QUERY` 工作区结果；系统消息、私有推理/原生保留项、工具参数/原始结果和图片 URL 不进入索引。实时防抖会话选择器搜索和工作区范围的 ACP session list 已实现；模型生成标题和 ACP 全文搜索方法仍待实现 |
| 类型化会话回合应用边界 | M3 | compatible | `RunTurnRequest`、`SessionTurnRunner` 和 `SessionTurnService` 由 `neuro_code.application.sessions.turns` 拥有；CLI、TUI、ACP 和应用消费者使用规范接缝，同时 package 与旧 session service 保留保持 identity 的兼容 re-export。回合锁、持久化上下文、取消、事件顺序和 Runtime 行为保持不变 |
| 类型化会话绑定应用边界 | M3 | compatible | `ConversationBinding` 与 `ConversationRunner` 由 `neuro_code.application.sessions.binding` 拥有；ACP、bootstrap、会话应用服务以及面向 Runtime 的消费者使用规范接缝，profile 与旧 runtime 路径保留保持 identity 的兼容 re-export。profile 专属会话选择与绑定替换、回合锁、取消和 Runtime 行为保持不变 |
| 类型化会话选择与策略投影 | M3 | compatible | `SessionOption`、`SessionSelectionResult`、`ReasoningEffortSelectionResult` 和 `InteractionModeSelectionResult` 由 `neuro_code.application.sessions.contracts` 拥有；TUI 消费规范投影，profile 与旧 runtime 路径保留保持 identity 的兼容 re-export。选择、策略应用、锁、恢复和 wire 行为保持不变 |
| 类型化交互式会话选择接缝 | M3 | compatible | `SessionSelectionController` 与非拥有型 `SessionSelectionService` 由 `neuro_code.application.sessions.selection` 拥有；TUI 通过门面执行列表/搜索/选择/重命名，`ProfileConversationController` 继续负责锁、绑定替换、工作区校验、恢复生命周期及兼容性的执行记录投影。会话 schema、Runtime、Provider、Finalizer 和 wire 行为保持不变 |
| 会话项目与会话库管理 | M3 | compatible | 会话可选归属一个项目；会话库 schema v35 在串行的 v1→v35 migration 链中新增 `session_projects` 表、规范化名称唯一索引以及可空的 `sessions.project_id` 列。`SessionProject` 与 `normalize_project_name()` 由 `neuro_code.domain.sessions` 拥有；`SessionLibraryService` 拥有有界的项目增删改查、任意会话重命名/删除、项目归属，以及用于工作区列表与开启全新会话的 `SessionLibraryOwner` 接缝。`ProfileConversationController.start_new_session()` 复用既有绑定替换路径，因此新会话在首个回合惰性持久化，原会话保持持久；删除当前打开的会话会被拒绝，删除项目会解除会话归属并清除项目记忆。TUI 的 `SessionLibraryScreen` 返回类型化动作，由控制器执行后再重新打开；`/sessions` 打开管理器，`/resume` 保持直接恢复。权限、沙箱、验证、监督、Runtime、Provider 与 wire 行为均无变化。参见 [ADR 0165](adr/0165-session-projects-and-library-management.md) 和 [ADR 0172](adr/0172-project-memory-v1.md) |
| Project Memory V1 | M3 | partial | 会话仅通过规范的 `SessionProject.id` 共享本地有界记忆；`cwd` 不会创建或选择记忆 scope，未归属项目的会话和默认 subagent binding 均不会获得记忆。state-root 文件适配器会校验 UUID scope、精确正文 ID、禁止跟随链接读取、目录包含性、文件名、数量和字节上限。每个项目包含 manifest、生成的有界 `MEMORY.md` 上下文证据 index，以及定义稳定且未绑定时 fail closed 的精确 ID 只读召回工具；请求注入为 synthetic，不会持久化。应用层会为 active context generation 固定 index snapshot，后台提取不会追溯改写当前前缀。新 Session/Resume、项目 attach/move/detach 和已提交 Fresh Context Rollover 会刷新；重命名保留快照，full compaction 不刷新。由组合根持有的提取 worker使用每项目生命周期锁、每会话 cursor、manifest 去重/更新、明确且有界的 remember/forget、禁用工具的 Provider 请求、严格来源/prompt/输出/事件/候选/队列/时长限制、类型化可观测结果及关闭取消。项目生命周期和删除语义与 v35 schema、Provider affinity、权限和沙箱所有权兼容。全局记忆、语义/向量/图检索、云同步和完整管理界面不在范围内。参见 [ADR 0172](adr/0172-project-memory-v1.md) |
| 有界用户附件 | TUI/CLI | compatible | 图片与文本文件经 `application/sessions/attachments.py` 附加到回合(图片单张 ≤5 MiB/总量 ≤10 MiB/每回合 ≤8 张,作为 data URI 的 IMAGE 部件;可解码文本文件单个 ≤64 KiB/总量 ≤128 KiB,内联进合成提示词).请求估算器把媒体部件投影为有界计量事实,并加每图固定 token 定额,因此大附件不会再虚增 `ContextPreflight`.TUI 提供 `/attach`、附件托盘,以及经 `ClipboardImageReader` 端口的 `Ctrl+V` 剪贴板贴图;CLI 提供可重复的 `--attach`;ACP 保持其内联提示词契约并共享同一套上限.附件是用户输入,不经过沙箱或审批,也不进入会话检索索引.参见 [ADR 0167](adr/0171-bounded-user-attachments.md) |
| 类型化持久会话生命周期边界 | M3 | compatible | `StartSessionRequest`、`ImportSessionRequest`、`RenameSessionRequest`、`ForkSessionRequest`、`DeleteSessionRequest` 与 `SessionLifecycleService` 由 `neuro_code.application.sessions.lifecycle` 拥有；Runtime 会话创建、CLI 导入/重命名以及 ACP 分叉/删除使用规范请求，旧 session service 通过保持 identity 的兼容导出进行委托。工作区可见性、binding 替换、回合锁、协议清理、schema、Runtime 执行、Provider、Finalizer 和 wire 行为保持不变 |
| 类型化会话任务查询边界 | M3 | compatible | `ListSessionTasksRequest`、`GetSessionTaskRequest`、`SessionTaskQueryController` 与 `SessionTaskQueryService` 由 `neuro_code.application.sessions.task_queries` 拥有；Runtime 排队计划查询和 `AgentConversation` 任务读取使用规范接缝，旧 session service 通过保持 identity 的兼容导出进行委托。任务写入、状态转换、锁、取消、Runtime、Provider、Finalizer、schema 和 wire 行为保持不变 |
| 类型化会话摘要查询边界 | M3 | compatible | `GetSessionSummaryRequest`、`SessionSummaryQueryController` 与 `SessionSummaryQueryService` 由 `neuro_code.application.sessions.summary` 拥有；会话恢复、bootstrap 配置、ACP 工作区校验和会话作用域工具输出 artifact 读取使用规范接缝，旧 session service 通过保持 identity 的兼容导出进行委托。生命周期写入、事件/会话项读取、schema、事务、Runtime、Provider、Finalizer 与 wire 行为保持不变 |
| 类型化执行记录查询边界 | M3 | compatible | `LoadExecutionRecordRequest`、`LoadExecutionRecordsRequest`、`SessionExecutionQueryController` 与 `SessionExecutionQueryService` 由 `neuro_code.application.sessions.execution_queries` 拥有；会话目录和会话恢复/重载读取使用规范的单条/批量接缝，旧 session service 保留保持 identity 的导出。执行记录写入、schema、事务、Runtime、Provider、Finalizer、TUI、ACP 与 wire 行为保持不变 |
| 类型化会话事件查询边界 | M3 | compatible | `LoadSessionEventsRequest`、`SessionEventQueryController` 与 `SessionEventQueryService` 由 `neuro_code.application.sessions.event_queries` 拥有；会话导出和会话作用域工具输出 artifact 读取共享复制后的不可变 mapping 投影，旧 session service 保留保持 identity 的导出。事件写入、解码、schema、事务、Runtime、Provider、Finalizer、TUI、ACP 与 wire 行为保持不变 |
| 类型化会话项查询边界 | M3 | compatible | `LoadSessionItemsRequest`、`SessionItemQueryController` 与 `SessionItemQueryService` 由 `neuro_code.application.sessions.item_queries` 拥有；会话恢复/重载和会话导出共享规范的有序 tuple 投影，旧 session service 保留保持 identity 的导出。会话项写入、计划、评论、事件、schema、事务、Runtime、Provider、Finalizer、TUI、ACP 与 wire 行为保持不变 |
| 规范 application 消费者导入 | M3 | compatible | Bootstrap composition 与 TUI 从具体的 `application.providers.service` 和 `application.workflows.*` owner 导入 Provider/工作流服务；CLI/bootstrap/ACP 与 CLI 序列化器从具体的 session lifecycle/service/catalog owner 导入。聚合 package export 仍保持 identity 的兼容路径。本阶段只改变导入，不改变 service 所有权、锁、持久化、Runtime、Provider、Finalizer、TUI 布局、ACP wire 或工作流行为；由于 plan/comment/export 没有第二个生产消费者，仍由现有 owner 负责 |
| 规范工具输出 artifact 导入 | M3 | compatible | CLI、TUI、ACP、bootstrap、ACP application 编排和 CLI 序列化器直接从 `neuro_code.application.tools.service` 导入有界 artifact application owner；package 聚合入口仍是保持 identity 的兼容路径。artifact 句柄、会话可见性、脱敏、字节上限、清理、权限、存储、Runtime 与协议行为保持不变 |
| CLI 会话命令边界 | M3 | compatible | `neuro_code.interfaces.cli.app` canonical 地负责 CLI parser、顶层 dispatch、展示和 exit-code handling；`neuro_code.interfaces.cli.sessions` 通过窄的 `SessionCliServices` contract 负责已解析的 `sessions` list/search/rename/compact/artifacts/recover operation。原根级 `neuro_code.cli` implementation 已删除。既有 JSON/plain output、validation/error、artifact bounds、recovery/compaction cleanup 和 `interfaces.cli.serialization` projection 不变。详见 [ADR 0152](adr/0152-cli-session-command-boundary.md) 和 [ADR 0153](adr/0153-architecture-completion.md) |
| Rust 会话导入 | M2 | partial | 已实现只读解析 v0/v1、新旧消息混排、结构化图片、有序推理/后端工具载荷、内嵌 `raw_output`/单体推理恢复与后端 ID 去重、有界损坏行恢复、SQLite 原子导入、内建沙箱 profile 与生成标题保留、自定义 profile 拒绝、前缀安全恢复、导出 v4、供应商原生图片回放、可信来源的 xAI Chat 回放，以及严格亲和的 Responses 原生加密/后端工具回放；压缩与有状态 ID 仍待实现 |
| 全屏/精简 TUI | M3/M4 | partial | Textual 界面已实现有界多行 Composer、安全 Markdown、带滚动跟随保护的稳定原地流式回答、F8 会话复制界面，以及随终端宽度展开、仅保留少量边距的对话/输入区容器。统一主题注册表提供暖瓷白、曜石黑及 system、Tokyo Night、Everforest、Ayu、Catppuccin 两个变体、Gruvbox、Kanagawa、Nord、Matrix、One Dark，共 13 个选项；设置 → 外观主题支持滚动预览、应用保存与取消恢复；输入区无额外标题，多行编辑区和发送操作行分开呈现；输入区与历史用户消息采用独立主题底色和细线边界，增强占位、光标及选区对比，system 通过终端前景色细线区分区域；发送复用原有提交流程；Ctrl+J / F2 和可用 Tab+Enter 操作的「换行」按钮提供换行入口，Alt+Enter / Shift+Enter 取决于终端透传。详见 [TUI 外观主题](tui-themes.md)。连续工具调用会形成一个默认折叠的活动组，摘要显示聚合数量、可见失败与耗时，并截断 Bash 意图；单击或 Enter 可查看有界脱敏的权限、完整命令、输出、artifact 与 diff。永久底部区域只保留 Composer，以及在有界模型/强度/模式和上下文/工作区区域之间分配空间的一行无冗余字段名状态；`/help` 与 F1 取代快捷键栏。克制设置与强度列表区分焦点和已选状态，Modal 使用小/中/大三档。既有六级强度、四种交互模式、计划/任务/后台唤醒流程、供应商/会话设置和发现、中英文界面、失败关闭审批、取消/重试/回退、视口校准、持久上下文、生命周期清理以及跨平台无头/生产终端测试继续保留。TUI 可在展开时读取会话作用域的有界 Bash 输出 artifact；其他输出来源的 artifact 仍待实现。详见 [ADR 0108](adr/0108-editorial-tui-presentation.md)。 |
| Markdown/Mermaid/媒体渲染 | M3/M5 | partial | 助手 Markdown 使用应用自有语义主题，不把模型文本解释为 Rich/Textual markup，并禁用链接点击；已渲染标题、强调、代码、列表、链接和表格。工具输出与差异使用应用赋予样式的字面文本。Mermaid 和内嵌媒体仍待实现 |

| 附加交互式终端会话 | M4 | partial | 普通本地 CLI/TUI binding 通过由 binding 拥有的 `InteractiveTerminalManager` 暴露 `create_terminal`、`terminal_output`、`terminal_write`、`terminal_resize`、`terminal_wait` 和 `terminal_kill`。会话仅限同一进程、仅存于内存，使用不透明 ID 与有界游标输出；binding 替换/关闭时会关闭会话，进程崩溃后不会恢复。TUI 只展示一个选中的会话，不成为终端模拟器。模型的 terminal-write 仍是有副作用操作，并同时经过现有 ToolExecutor 与 manager 权限边界。ACP、子代理、planner 和 UltraCode worker 不会获得这组本地工具；ACP client-terminal 行为不变。附加终端不是验证证据 |
| PTY 与进程树对齐 | M3/M4 | partial | 前台/后台命令共享受控 `ProcessTree`；POSIX 会等待仍存活的进程组，Windows 则在关闭即终止的 Job Object 中原子创建入口、限制继承的标准句柄、在入口退出后等待后代，并使用基于句柄的整树终止。新的共享交互式终端领域/端口和管理器提供有界且按游标寻址的尾部输出、显式截断、输入/resize/信号/等待/关闭、权限/工作区/沙箱门禁、取消安全创建与注册表关闭。POSIX 会话持有 PTY 进程组；生产 Windows 会话原子组合 ConPTY 与 Job 列表属性，并终止整个 Job。可移植属性/句柄/错误测试、真实 Linux PTY 测试、原生进程树测试和选择性三平台 CLI 终端冒烟覆盖该边界。ACP 还支持由能力协商控制的非交互式客户端前台执行，以及有界标准直接可执行文件后台生命周期；不提供交互式 PTY framing/背压、输入或 resize |
| 操作系统沙箱配置 | M3 | partial | 已实现规范的 `off`/`workspace`/`read-only`/`strict` 配置与 CLI 覆盖、防止项目弱化用户 profile、针对 Bash/后台 Bash/stdio MCP/LSP/启用 profile 的 PTY request 的 Linux child-scoped Bubblewrap 和 macOS child-scoped Seatbelt、私有 child HOME/TMP 与环境白名单、request-scoped POSIX inode 审计、显式 close-FD 边界、只读编辑 schema/runtime 门禁、profile-specific network enforcement、可信 helper 校验、不支持平台的失败关闭、无 skip 的专用 Linux 与 ARM64/Intel macOS security CI、原生 Windows Job/ConPTY CI，以及会话固定 profile 保存/恢复冲突检测；`off` 明确不提供 OS 隔离；macOS detached-descendant lifecycle 仍为 best effort；由于当前没有 Mac 硬件，物理 SIP-enabled macOS 验收标记为 `DEFERRED_NO_MAC_HARDWARE`，这不是 code blocker，也不表示验收已通过；Windows W3 为启用的非 PTY `workspace` 和 `read-only` request（包含 protocol LSP child）提供 focused-acceptance-certified provider；`strict` 因无法提供 strong read isolation 而继续失败关闭。W4 PTY/ConPTY 已通过 focused 原生验收；CI run `32374860136` 的 W5 workload matrix 已通过 W3/W4 的全部 20 行。详见 [ADR 0111](adr/0111-macos-seatbelt-local-process-sandbox.md)、[ADR 0112](adr/0112-windows-appcontainer-sandbox-feasibility-decision.md)、[ADR 0114](adr/0114-windows-native-sandbox-runtime.md) 和 [ADR 0116](adr/0116-windows-developer-workload-compatibility.md) |
| Windows AppContainer 沙箱可行性 | M3 | partial | 已研究的 classic stable unpackaged AppContainer 文件系统/ACL authority、secured named-pipe IPC、runtime 与 standard-user primitive 单独可行。current stock Git for Windows 在不扩展受保护 ancestor ACL、不捆绑 patched Git、也不使用未沙箱化 broker 的前提下，仍无法兼容完整的非管理员 repository workflow；这些替代方案均被拒绝。Windows `off` 继续通过现有 Job Object/ConPTY 路径支持，并报告 `STRONG_DESCENDANT_OWNERSHIP`；启用的 `workspace`、`read-only` 和 `strict` profile 仍为不支持 / 失败关闭。Evidence PR #33--#39 未合并。详见 [ADR 0112](adr/0112-windows-appcontainer-sandbox-feasibility-decision.md) |
| Windows 原生 restricted-token 与 setup authority | M3 | partial | W1 增加平台无关的文件系统/网络 `LocalProcessSecurityCapabilities` 轴 `READ_ISOLATION`、`WRITE_ISOLATION` 和 `NETWORK_ISOLATION`，使用显式 `STRONG`/`LIMITED`/`UNSUPPORTED` 强度和失败关闭的 satisfaction。W1/W2 仍是 primitive 和 setup authority，其 foundation actual capability constant 保持 `UNSUPPORTED`。完整接通的 W3 非 PTY runtime 提供由 focused 原生验收认证的 limited read、strong write、strong network contract；strong-read request 不能由 limited provider 满足。Gate 1–5 执行 7 个 native acceptance test 且 0 skip。Process lifecycle 继续由独立的 `LocalProcessLifecycleCapability` contract 负责，W3 runner Job 以及现有 Job Object/ConPTY path 报告 `STRONG_DESCENDANT_OWNERSHIP`。W2 provisioning 真实 `NeuroSandboxOffline`/`NeuroSandboxOnline` local user，持久化其 SID 和实际 credential，并在 DPAPI machine-scope record 上设置只针对两个 user 的 exact NTFS deny ACE；installation-scoped synthetic SID 只作为 restricted-token 的仅写 principal，不能作为 read 或 firewall identity。native ACL reconciliation 使用 `SetEntriesInAclW` 保证 deny-before-allow canonical order，保留无关 controller ACE，并修复 exact read/primary-user-write/restricting-write/read-only-deny/sensitive-deny tuple。安装实例拥有一个只按真实 Offline SID 限定的持久 Offline Firewall block rule；Online identity 的使用不会删除它，只有显式 cleanup 才会删除该 rule。setup state 为 `READY`/`NEEDS_SETUP`/`NEEDS_REPAIR`/`UNSUPPORTED`，并保持管理员 setup boundary 与非管理员 runtime fact。CI run `32374860136` 的 W5 workload matrix 已通过 W3/W4 全部 20 行且未改变该 contract。详见 [ADR 0113](adr/0113-windows-native-restricted-token-sandbox-architecture.md)、[ADR 0114](adr/0114-windows-native-sandbox-runtime.md) 和 [ADR 0116](adr/0116-windows-developer-workload-compatibility.md) |
| 本地进程生命周期能力契约 | M3 | compatible | `LocalProcessLifecycleCapability` 显式区分 `STRONG_DESCENDANT_OWNERSHIP` 与 `PROCESS_GROUP_BEST_EFFORT`；唯一 satisfaction helper 允许强适配器满足 best-effort request，并让 best-effort 适配器在创建 child 前对强要求失败关闭。Linux enabled Bubblewrap 与 Windows Job Object/ConPTY 路径报告 strong ownership；普通 POSIX 与 macOS Seatbelt 报告 best effort。文件系统/网络权限保持正交。详见 [ADR 0110](adr/0110-cross-platform-lifecycle-capability-contract.md) |
| ACP stdio/WebSocket | M4 | partial | 官方 ACP stdio SDK 与有界 WebSocket newline-JSON bridge 已实现版本协商、session 生命周期、有界/脱敏回放、能力门控的文件/终端、内嵌 image/audio/blob prompt，以及私有 artifact/subagent/lifecycle/MCP/compaction 扩展。stdio、Streamable HTTP、legacy SSE MCP server 可接入；ACP-transport MCP server、二进制多媒体历史回放和交互式 PTY input/resize/framing 仍不支持。`neuro_code.interfaces.acp.agent` 作为 canonical owner 负责 ACP protocol Agent，并把 transport、content、event projection、client I/O、无状态 MCP declaration conversion 和 per-session state 委托给明确的 ACP 子模块。原根级 `neuro_code.acp` implementation 已删除。`ConversationBinding.close()` 继续是 binding cleanup authority，`SessionTurnService`/`ConversationRunner` 继续拥有 turn/recovery authority，wire behavior 不变。详见 [ADR 0146](adr/0146-acp-update-and-event-projection-boundary.md)、[ADR 0147](adr/0147-acp-client-io-adapter-boundary.md)、[ADR 0148](adr/0148-acp-mcp-configuration-boundary.md)、[ADR 0150](adr/0150-acp-session-runtime-ownership-boundary.md)、[ADR 0151](adr/0151-acp-transport-boundary.md) 和 [ADR 0153](adr/0153-architecture-completion.md)
| MCP 服务器 | M4 | partial | 已集成官方 MCP ClientSession、工具 schema/协商/调用，以及 resources、resource templates、prompts、sampling、elicitation 和有界动态刷新，覆盖 ACP 提供的 stdio、Streamable HTTP 与 legacy SSE server。无状态 ACP MCP declaration conversion 由 `neuro_code.interfaces.acp.mcp_config` 作为 canonical owner；它不执行 I/O，保留 protected-environment rejection 与既有有界 configuration/error contract，并返回既有 application config。资源/提示读取和 sampling/elicitation callback 通过私有有界 ACP extension 暴露，动态工具目录刷新也已通过同一 session-owned path 实现；这些是有界投影，不等于通用 MCP parity。持久化 MCP 配置与 ACP-transport MCP server declaration 仍不支持。ACP stdio/WebSocket server transport 已在上一行单独说明。详见 [ADR 0148](adr/0148-acp-mcp-configuration-boundary.md)
| 技能与 AGENTS.md | M4 | partial | 已实现精确文件名 `AGENTS.md` 的有界继承发现，以带标记的临时 User 上下文注入，并在每个模型步骤前按绑定内目标刷新；`search_replace` 会对比最近一次实际注入的路径/内容快照，遇到模型未见的变更即中止。只读技能发现覆盖嵌套 LOCAL、从工作区上方到 git 根的每个 REPO 祖先以及 USER 主目录，采用确定性的 `LOCAL > REPO > USER` 遮蔽、遍历/条目/文件/字节/目录硬上限、拒绝所有链接/reparse point，并使用正文敏感指纹。有界 `skill` 工具会校验发现指纹、剥离 frontmatter、安全采样捆绑文件名，并支持有界的 `$ARGUMENTS`、位置参数及 `${SKILL_DIR}` 替换。其他指令文件名/rules、`.cursor` 与供应商默认兼容、条件技能、远程/服务器/内置技能、hooks/plugins 和子代理集成仍待实现 |
| 钩子与插件 | M4 | planned | 钩子和插件市场 crate |
| 子代理与计划模式 | M4 | partial | 持久结构化计划、TUI/CLI/ACP 显式计划与子代理面、生命周期投影仍保留。所有生产 child-runtime path 现在都接收实际 parent binding capability manifest：CLI 会打开 parent binding，TUI 读取活动 binding，ACP 要求活动 parent binding。显式只读 path 把固定工具名作为 request，在 binding 构造前解析 `parent ∩ requested ∩ composition-owned global policy`，并校验 binding manifest 与 runtime fingerprint。有界应用 scheduler 仍提供每次尝试创建新 child runtime、最大并行数、重试、超时、递归/depth gate、tool allowlist、读写能力检查、顺序结果与清理。任意旧 executor 接缝仅供测试/内部兼容使用；关系 resume/fork/delete 不会重新构造 Runtime。本项只证明 `child capability <= actual parent capability`，不等于完整证明 Permission/Workspace/Sandbox/MCP/Agent security system。父上下文复用与无界自动编排仍在本行之外。CLI/TUI/ACP 的显式子代理入口仍只读。另有内部有界 Writable Subagent path，仅在真实 parent capability、requested worker capability、composition-owned global policy、managed worktree、sandbox/workspace constraints 与 worker policy 都允许时提供 `search_replace`/`apply_patch` 写工具；有界 Task DAG 与 Ultracode 的 `BOUNDED_SWARM` 分支可通过既有 factory 使用新的 scoped worker。这不表示普通 subagent 可写，也不授予任意 Bash、network 或 MCP 访问。详见 ADR 0131 与 ADR 0141
| 显式子代理生命周期 CLI 控制 | M4 | partial | Stage5CZ 新增 `neuro subagents ACTION TASK_ID --parent-session SESSION_ID`，通过 application 生命周期 owner 执行显式 `resume`、`fork` 和 `delete`。普通和 typed JSON 投影只包含有界生命周期标识；命令不运行模型、不重放工具、不自动打开 fork，也不直接读取 SQLite。 |
| 显式 ACP 子代理生命周期扩展 | M4 | partial | Stage5DA 新增私有 `_neuro-code/session/subagents`，请求严格包含外部 `sessionId`、有界 `taskId` 以及 `resume`/`fork`/`delete` 动作。Resume/fork 返回外部 ACP alias，delete 只返回 deleted 标志；模型回合、工具重放、递归/并行子会话、可写工具、内部 ID 和子会话正文不会跨 wire。 |
| 失败关闭的 ACP 子代理生命周期投影 | M4 | partial | Stage5DB 校验返回的父 session/task/action 关联，并在 wire 序列化前限制非 delete 外部 alias。错误的注入结果会失败关闭；有效生命周期响应及 Runtime/会话行为保持不变。 |
| ACP 子代理 alias 重连兼容性 | M4 | partial | Stage5DC 将生命周期 alias 分配限制为四次，在序列化前把每个 alias 解析回预期子会话，针对冲突或错误所有者结果重试，并在重复 resume 请求和 ACP 客户端重连后保留持久 alias。私有扩展、子代理执行、schema 和 ACP 标准 capability 集保持不变。 |
| 确定性上下文压缩评估 | M5 | partial | Stage5DD 新增 canonical `neuro_code.application.memory.compaction` planner，提供类型化用量、软/硬阈值、受保护前缀和近期后缀边界以及仅包含索引的有界计划。未知容量明确返回 `UNAVAILABLE`；不生成摘要、不调用 Provider、不创建持久条目、不接入 Runtime，也不改变会话/UI/wire 行为。 |
| Provider 感知的上下文摘要请求边界 | M5 | partial | Stage5DE 在 canonical memory 模块新增有界 `ProviderContextWindow` 元数据与 `ContextSummaryRequest` 投影。已知容量会裁剪摘要预算；未知容量、不可执行计划和空候选区间失败关闭。不增加 Provider 参数、不调用模型、不创建提示词或持久条目，也不接入 Runtime 或改变接口行为。 |
| Provider 感知的脱敏摘要输入 | M5 | partial | Stage5DF 新增 `ContextSummaryInputBuilder` 与类型化的有界投影。它会在控制字符/UTF-8 限制前处理显式值和常见凭据形状，省略工具参数、推理和保留的 Provider 载荷，并使用注入的本地 token 估算器受 Provider 窗口剩余预算约束。不调用 Provider、不构建提示词、不修改上下文、不持久化压缩条目，也不改变 Runtime/接口行为。 |
| Provider 驱动的有界上下文摘要生成 | M5 | partial | Stage5DH 新增 `ProviderContextSummaryGenerator` 和 `ContextSummaryGenerationResult`。它只接受脱敏且有界的输入，校验 Provider/model 亲和性，构建临时提示词，恰好使用 `DISABLED` 无工具请求，缓冲增量并优先使用 `ModelCompleted.response_text`，再次脱敏/限制结果；缺少/空/多个完成事件或远端工具事件会失败关闭。不启用自动压缩、重试、持久化、事件或整轮原子性。 |
| 记忆与上下文压缩 | M5 | partial | Stage5DD 建立确定性评估；Stage5DE/DF 增加 Provider 感知的有界请求/输入契约；Stage5DG 增加持久化压缩条目和显式恢复重建；Stage5DH 增加单请求严格无工具摘要生成；Stage5DI 增加带调用前源指纹校验并委托 `SessionStore` 的显式应用持久化服务；Stage5DJ 固定兼容边界：压缩行不进入导入/导出和分叉复制，`finalize_turn` 只对自身的事件/条目/搜索/执行记录事务提供原子性。Runtime 自动触发和有界事件仅在 ADR 0107 的受控、已持久化且 Provider 容量已知条件下启用；Provider 生成与压缩持久化仍是分离的事务。 |
| 显式上下文压缩触发 | M5 | partial | Stage5DK 新增默认关闭的 `ContextCompactionTriggerService` 应用层边界。`DISABLED` 只进行纯评估；`EXPLICIT` 只有在调用方提供过期源元数据后才委托可执行计划。触发服务不改变 Runtime、Provider、SessionStore schema、导入/导出、事件、CLI、TUI 或 ACP 行为，也不复用普通回合预算。见 [ADR 0091](adr/0091-explicit-context-compaction-trigger.md)。 |
| Runtime 压缩安全边界门控 | M5 | partial | Stage5DL 新增带 `BEFORE_MODEL_REQUEST`/`AFTER_TOOL_BATCH` 安全位置的类型化门控、活动操作/取消失败关闭检查以及独立的单请求/无工具预算契约。不接入 `AgentRuntime`、不新增事件、不启用自动触发、不实现超时，也不宣称整轮事务原子性。见 [ADR 0092](adr/0092-runtime-compaction-safe-boundary.md)。 |
| 强制执行 Runtime 压缩超时 | M5 | partial | Stage5DM 新增有限且真正执行的墙钟限制（默认 30 秒，最大 300 秒），覆盖允许的显式压缩摘要生成和持久化调用。超时抛出 `ContextCompactionTimeoutError`；Provider 错误、存储错误和取消保持不变。不接入 `AgentRuntime`、不启用自动压缩，也不宣称跨操作事务原子性。见 [ADR 0093](adr/0093-enforced-context-compaction-timeout.md)。 |
| Runtime 压缩失败投影 | M5 | partial | Stage5DN 新增 `classify_context_compaction_failure()`，将超时有界投影为可恢复的 `BUDGET_LIMITED/WALL_TIME_BUDGET`（`finalized=false`），并指定 `TURN_FINALIZATION` 记录策略。取消、Provider 和存储失败仍只传播；未知异常保持未分类。投影不保存错误详情，不修改 `AgentRuntime`、事件或自动压缩，也不声称跨操作事务原子性。见 [ADR 0094](adr/0094-runtime-compaction-failure-projection.md)。 |
| 显式 Runtime 压缩接缝 | M5 | partial | Stage5DO 新增可选且默认关闭的 `AgentRuntime.compaction_runtime_gate` 与 `trigger_context_compaction()`，接收调用方完整提供的安全边界请求。缺少配置时失败关闭；委托不会修改上下文、普通回合 steps、事件或执行记录。`AgentRuntime.run()`、Composition、自动触发和生产 gate 组装保持不变。见 [ADR 0095](adr/0095-explicit-runtime-compaction-seam.md)。 |
| 由应用层拥有的显式压缩调用方 | M5 | partial | Stage5DP 为每个 `ApplicationComposition` binding 组装带脱敏边界的 `ContextCompactionRuntimeGate` 并注入 `AgentRuntime`，但不启用自动触发。`AgentConversation.trigger_context_compaction()` 是显式调用方，使用现有会话回合锁串行化压缩，对 `EXPLICIT` 请求要求匹配的持久化会话，并委托调用方拥有的不可变快照。transcript、事件、普通回合预算和 `finalize_turn()` 事务性保持不变。见 [ADR 0096](adr/0096-application-owned-compaction-caller.md)。 |
| 带压缩的回合最终化原子事务 | M5 | partial | Stage5DQ 增加可选的 `SessionStore.finalize_turn_with_compaction()`；SQLite 原子提交完成事件、会话条目/搜索投影、可选执行记录和一个持久化压缩行。普通 `finalize_turn()` 与 `save_compaction_item()` 保持不变，Provider 生成不在事务内，当前还没有 Runtime 或自动压缩调用方使用该边界。详见 [ADR 0097](../en/adr/0097-atomic-turn-finalization-with-compaction.md)。 |
| 回合记录器压缩最终化所有者 | M5 | partial | Stage5DR 允许 `TurnEventRecorder.finalize_turn_completion()` 使用预先校验的 `DurableCompactionItem` 选择原子存储方法；普通调用保持不变，非法输入会在内存完成事件加入前失败，当前 Agent loop 和自动触发器都不会提供该条目。Provider 生成和失败投影仍由调用方拥有。详见 [ADR 0098](../en/adr/0098-turn-recorder-compaction-finalization-owner.md)。 |
| 类型化压缩回合投影 | M5 | partial | Stage5DS 新增 `ContextCompactionTurnProjection`：成功触发只转移已持久化且校验过的条目；超时携带有界可恢复预算 outcome；取消、Provider、存储和未知失败不会被静默消费。不启用事件、持久化、自动压缩或普通 loop 行为。详见 [ADR 0099](../en/adr/0099-context-compaction-turn-projection.md)。 |
| 显式压缩回合所有者 | M5 | partial | Stage5DT 新增可选的 `TurnEventRecorder.finalize_turn_from_compaction_projection()` 接缝。成功投影要求调用方提供普通 outcome 并使用回合加条目的原子存储；超时只消费自身有界 outcome；无操作和只能传播的投影失败关闭。普通 loop 和自动压缩保持不变。详见 [ADR 0100](../en/adr/0100-explicit-compaction-turn-owner.md)。 |
| 在回合锁下由应用层拥有压缩 | M5 | partial | Stage5DU 新增 `AgentConversation.run_context_compaction_with_owner()` 及对应的 `ConversationRunner` 协议方法。它校验调用方拥有的不可变请求，在现有会话回合锁下串行化门控与所有者，只消费成功或受控超时投影，并在调用所有者前对无操作投影失败关闭。取消、Provider、存储和未知失败保持传播；普通 loop、自动压缩、事件和 UI 保持不变。详见 [ADR 0101](../en/adr/0101-application-compaction-owner-under-turn-lock.md)。 |
| 上下文用量快照与过期源请求构造 | M5 | partial | Stage5DV 新增无副作用的应用层辅助入口：有 Provider 输入/输出用量时使用报告值，否则使用有界 `ModelContext` 估算；未知 Provider 容量保持未知；仅对可执行的显式请求计算过期源指纹。它们不调用 Provider/存储、不修改上下文，也不启用自动压缩。详见 [ADR 0102](../en/adr/0102-context-usage-snapshot-and-stale-source-builder.md)。 |
| 显式实时上下文压缩命令 | M5 | partial | Stage5DW 新增 `AgentConversation.run_explicit_context_compaction_with_owner()`。它在现有会话锁内构建带实时指引的上下文，委托已配置 Runtime gate 构造 usage/过期源保护，并生成有界身份/时间元数据后复用 typed owner 投影。要求已有持久化会话，仍是显式操作：不接入普通 loop、不自动触发、不发事件、不修改 transcript，也不把 Provider 与存储放入同一事务。详见 [ADR 0103](../en/adr/0103-explicit-live-context-compaction-command.md)。 |
| 显式压缩命令投影 | M5 | partial | Stage5DX 新增 `ContextCompactionCommandResult` 以及有界 CLI/ACP 序列化器。`completed`、`not_needed` 和受控 `budget_limited` 超时结果只暴露不透明 ID、计数和摘要 token 元数据；Provider、取消、存储和未知失败仍然是异常。不启用命令、事件、普通 loop 接入或自动触发。详见 [ADR 0104](../en/adr/0104-explicit-compaction-command-projection.md)。 |
| 统一普通执行预算与 REPLAN 指引 | M3/M5 | partial | 正式 CLI/TUI/ACP 入口默认使用应用层拥有的 `normal` 48/48/192 模型/工具轮次/工具调用档位，`deep` 使用 96/96/384。旧 `--max-steps N` 现在把全部计数型普通上限同步映射为 N/N/4N；Finalizer 尝试继续独立。在受控模式下，批次结束的 `REPLAN` 决策会注入不持久化的合成指引直到产生新进展，每次请求还会收到与 Provider 无关的 batch-first 指引。现有顺序工具执行和 stuck 检测保持不变。详见 [ADR 0105](../en/adr/0105-unified-execution-budget-and-replan-guidance.md)。 |
| 有界长任务 Runtime | M3/M5 | partial | 受控回合现在会向模型和有界接口事件投影类型化预算压力；当会话已持久化且 Provider 上下文窗口已知时，在安全边界自动压缩；并在不重置全局执行预算的前提下发出有界进展分段检查点。标准 TUI 有意不展示原始执行计数。合成预算/检查点指引和压缩请求不进入规范会话。压缩 Provider 生成与持久化是分离操作，分段检查点是审计标记而非崩溃恢复或工作区回滚点。有界 Ultracode 委派是独立入口，不改变此压缩合约。详见 [ADR 0107](../en/adr/0107-bounded-long-task-runtime.md)。 |
| 持久化回合崩溃 / INDETERMINATE 恢复 | M5 | partial | schema 14 引入规范 `session_turn_attempts` 恢复索引；其后依次加入 schema 15→16 writable-subagent lease 保留迁移、schema 17 有界 Parent Relay 表、schema 18 持久化 Task DAG 表、schema 19 持久化 Leader attempt/decision 投影、schema 20 有界 predecessor-result Relay 表、schema 21 Task DAG recovery-claim fence、schema 22 有界 DAG capacity/scoped lease policy、schema 23 逐节点 execution-owner identity、schema 24 parallel-aware Leader decision projection、schema 25 有界 model-planning attempt/proposal 投影、schema 26 有界 DAG replan attempt/proposal 投影、schema 27 有界 Agent Swarm run projection、schema 28 有界 Ultracode delegation projection、schema 29 有界 Result Adoption projection、schema 30 nullable MAIN_MAX verification snapshot column、schema 31 durable Working Set row、schema 32 durable active-context generation marker 与 schema 33 canonical generation boundary。SQLite 会话事件存储行记录当前版本。request/output/tool write-ahead 标记、原子 turn finalization、plan-task ownership、有界 inspect/retry 规则以及 `INDETERMINATE` 永不自动 replay 的行为保持实现。后台唤醒输入、回合中途续接、工具补偿、工作区回滚和子进程协调仍不支持。详见 [ADR 0127](adr/0127-durable-turn-crash-recovery.md)。 |
| 只读 LSP 语义层 | M5 | partial | 已实现 Provider 无关的 `lsp` 工具，支持 definition、references、hover、document/workspace symbols、diagnostics、status 与有界 restart。它使用显式 `[lsp.servers.<name>]` argv profile，通过规范 local-process port 使用 Content-Length JSON-RPC，提供有界文档同步、UTF-8/UTF-16/UTF-32 协商位置、工作区/权限过滤和真实 stdio fake-server conformance suite。rename、format、code action 与 workspace edit 仍不支持；显式 writable-worker 组合由 ADR 0132 单独规定。详见 [ADR 0128](adr/0128-read-only-lsp-vertical-slice.md) |
| 受管 Git Worktree 能力 | M5 | partial | 应用拥有的本地生命周期现在支持类型化 repository identity、以精确 base commit 创建 detached 或 `neuro/worktree/<id>` branch、独立 `worktrees.db` durable ownership、bounded status、进程退出后的显式 reconciliation、规范 workspace binding（不继承 additional roots）和安全的非 force 移除。由于 filter preflight 使用 `git check-attr --source=<tree-ish>`，要求 Git 2.40.0 或更高版本；更低版本在初始化时失败关闭。dirty、locked、unmanaged、缺失和 mismatch worktree 都失败关闭。Checkpoint/rollback 是独立的内部 capability；显式单 child writable workspace 由 ADR 0131 单独规定；有界 Task DAG worker 可以使用独立的 managed Worktree，而 integration、branch deletion 和面向模型的 Git tool 仍不支持。详见 [ADR 0129](adr/0129-managed-git-worktree-capability.md) |
| 受管 Workspace Checkpoint / Rollback | M5 | partial | 内部 application service 现在为 READY 且由 Neuro Code 拥有的 managed worktree 捕获不可变、identity-bound、有界 source projection：exact Git index bytes、tracked change/deletion/mode/binary/symlink 与 non-ignored untracked file。独立 `checkpoints.db` 与 state-owned content-addressed artifact 使用 CAPTURING/READY publication、SHA-256 integrity、insert-only/CAS、exact-leaf rollback、ignored-file preservation、HEAD match、durable rollback attempt、Git worktree lock、final fingerprint verification 与 crash reconciliation。真实 child process 在 partial file effect 或 index restoration 后可由正常 reconciliation 收敛；active artifact corruption 转为 durable `INDETERMINATE`，不执行 restore 且不释放 owned lock；真实 rollback/rollback 与 rollback/remove race 失败关闭。自动 policy、model/TUI/ACP tool、history rewind、commit/patch/merge integration 和 source-checkout rollback 仍不存在。最低 Git 版本继承 Worktree boundary，为 2.40.0+。详见 [ADR 0130](adr/0130-managed-workspace-checkpoint-rollback.md) |
| 串行受管 Writable Subagent 工作区 | M5 | partial | 显式内部 service 只有在读取 parent exact committed HEAD、创建 Neuro Code managed branch worktree 并捕获 READY baseline checkpoint 后，才派生 typed grant。Child 使用全新 session/binding，唯一 root 是该 worktree；工具只有有界 read set 加 `search_replace`/`apply_patch`，可选只读 `lsp` 必须满足真实 parent/global/bounded-worker 交集。Bash、terminal、background、MCP、network、Git/worktree/checkpoint/rollback 与递归 subagent authority 均不存在。Durable lease 使用 ALLOCATING/WORKTREE_READY/BASELINE_READY/ACTIVE/PRESERVED/ORPHANED/FAILED、insert-only/CAS 和 reconciliation；不自动 rollback、移除、merge、commit、copy-back 或 cleanup。Standalone service 本身不是 public writable subagent surface；CLI/TUI/ACP 的显式子代理入口仍只读。有界 Task DAG 通过 typed factory 创建独立 scoped Writable service；automatic Ultracode 的 `BOUNDED_SWARM` 分支通过 Swarm → Leader → Task DAG 到达这些 worker。Ultracode router 本身不增加 writable authority；每个 lower worker 仍受真实 parent/request/global capability intersection、managed worktree 与 sandbox/workspace constraints 限制。无界并行可写 worker、integration、branch deletion 与面向模型的 Git tool 仍不支持。详见 [ADR 0131](adr/0131-managed-writable-subagent-workspace.md) |
| Worker-scoped Worktree + LSP runtime 集成 | M5 | partial | standalone writable runtime 从 managed handle 派生 `WorktreeWorkspaceBinding`，并要求 child binding/capability/worktree/LSP root 精确相等且没有 additional root。Parent 与各 worker 不共享 manager/client/route/document state；真实 write-tool 变更会在语义读取前重新同步。LSP 使用 child `LocalProcessSandbox`、保持只读、过滤 outside URI、拒绝 `workspace/applyEdit`，并在成功、失败、取消或超时后关闭 process/cache，同时保留 worktree/checkpoint/lease evidence。有界 Task DAG worker 各自保留独立 LSP manager/process/root；integration/merge、cleanup、UI 暴露与 writable LSP mutation 仍不存在。详见 [ADR 0132](adr/0132-worker-scoped-lsp-runtime-integration.md) |
| 有界 Parent Context Relay | M5 | partial | 每个显式 writable worker 现在接收一个只从实际 parent binding durable session 派生的确定性不可变快照。它在 10 项、每项 4 KiB、投影 24 KiB、渲染 32 KiB 边界内选择经配置脱敏的真实 USER/可见 ASSISTANT 纯文本；按结构排除 reasoning、tool call/argument/result、media、system 与 synthetic runtime context；绑定精确 worker 权限 identity 但不扩大权限；在 runtime/model 执行前写入一条 insert-only、完整性可验证的 schema-17 记录，并由当前 schema 保留；每次请求只注入一条字节稳定 synthetic Relay，且不写入 child history。Compaction summary 复用、live/shared context、无界自动委派与其他更丰富 result Relay 仍不存在。有界 Task DAG worker 各自接收独立 Relay，不共享可变 parent context。详见 [ADR 0133](adr/0133-bounded-parent-context-relay.md) |
| 有界持久化并行 Task DAG | M5 | partial | ADR 0134 增加显式 static typed DAG：最多 8 个节点、16 条边、每节点 4 个依赖，只允许 `WRITABLE_SUBAGENT`；按 ordinal/node ID 确定性选择 ready。Parent identity 来自真实 `ConversationBinding`；复用既有 `SessionTask`、Writable Subagent、Worktree、Checkpoint、Parent Relay 和 worker-scoped LSP owner。`max_parallel` 不可变、默认 1、上限 4。Schema 18 定义与 node projection、schema 20 predecessor-result Relay、schema 21 recovery claim、schema 22 capacity/scoped lease policy、schema 23 逐节点 execution-owner identity 与 schema 24 Leader decision projection 均保留，并使用 graph/node generation CAS。持久化 `RUNNING` node rows 是 canonical active set；旧 `active_node_id` 只是兼容 projection。每个节点通过 typed factory 获得新的 Writable service，保留每 worker lock，并拥有独立 worktree/session/lease/checkpoint/Parent Relay/LSP。依赖只控制调度；失败后代进入 `SKIPPED`，独立分支继续，不自动 retry 或 rerun。Fan-out Relay 精确绑定 target generation，fan-in 等待所有声明的 direct predecessor。Live execution owner 与 schema-21 recovery claim 保护跨进程启动/恢复窗口；allocation 后不确定仍为 `INDETERMINATE`，永不自动 rerun。真实 `spawn` acceptance 覆盖 provider overlap、K=2 capacity race、独立 managed resource、同一 snapshot 的双 controller 竞态与逐节点不重跑。model 生成 decomposition、dynamic/无界 dataflow、自动委派、merge、rollback、cleanup 与 UI/ACP 仍不属于本切片；内部有界 Swarm 组合由 ADR 0140 单独规定。详见 [ADR 0134](adr/0134-durable-serialized-task-dag.md) 与 [ADR 0136](adr/0136-bounded-task-dag-predecessor-result-relay.md) |
| 有界 DAG predecessor result relay | M5 | partial | ADR 0136 实现每个 dependent node generation 一个确定性 Relay：只包含直接、按声明顺序排列、状态为 `COMPLETED` 的 predecessor projection，并限制每项 4 KiB、合计 16 KiB、渲染 24 KiB。Schema-20 row 绑定精确 DAG/target definition、generation、worker task/session、保留中的 writable lease、worktree、baseline checkpoint 和 Parent Relay；它们 insert-only、完整性校验、精确重复幂等，对缺失、过期、篡改或冲突 evidence 失败关闭。Schema 21 增加绑定同一 node/parent-task/Relay identity 的独立 recovery-claim fence；schema 22/23 保留有界 capacity 与 execution-owner evidence。只有匹配的 task/lease/link 均不存在且没有 live recovery owner 时，read-only `SAFE_NOT_STARTED` 才允许复用；`RECOVERY_OWNED` 保护 live 或未被证明死亡的 owner，包括 lease-only partial window；lease allocation 前死亡的 owner 可通过 exact CAS takeover。Loser 不能启动 provider，也不能写 `FAILED`/`INDETERMINATE`；allocation 后歧义保持 fail-closed 且不自动 rerun。消息是独立 synthetic context channel，不能授予 tool、path、capability、workspace state 或 authority。真实 process-death acceptance 证明 Relay identity 不变、两个 controller exclusive ownership、B 只分配一次以及 allocation 后不 replay；并发 fan-out 中 B/C 的 target relay 仍各自精确。merge、retry 和 UI/ACP 暴露仍不存在。详见 [ADR 0136](adr/0136-bounded-task-dag-predecessor-result-relay.md) |
| 有界串行 Leader controller | M5 | partial | ADR 0135 增加一个只作用于预先创建 Task DAG 的显式 Leader，作为串行 compatibility path。它先 reconciliation DAG，再审阅有界、脱敏、确定性的 evidence envelope；zero-tool Leader model 允许发布 typed `SELECT_NODE` 或仅 terminal 可用的 `FINALIZE`。每个 provider request 绑定精确 DAG generation、definition/evidence fingerprint、Leader session、turn、owner 与 durable attempt identity。过期 `CLAIMED` 只有在没有 output、decision 和旧 session 匹配 turn evidence 时才可接管，并原子 rebinding 新 session/turn；持久化 `PROVIDER_FENCED` CAS 阻止仍存活的 stale owner 调用 provider，fence 之后的 recovery 失败关闭。Committed output 与 decision 保留历史 L1 provenance，由 fresh composition service 复用且不 replay provider。Leader 只调用既有 Task DAG one-step seam，`WRITABLE_SUBAGENT` 继续是 worker authority，DAG 继续是依赖/串行合法性 authority。dynamic/无界 dataflow scheduling、merge、rollback 与 UI/ACP 暴露仍不属于本切片；bounded Swarm composition 由 ADR 0140 单独规定，Ultracode entry 由 ADR 0141 规定但不由本 Leader 直接调用；有界 parallel-aware wave 详见 ADR 0137。详见 [ADR 0135](adr/0135-bounded-serialized-leader-controller.md) |
| Parallel-aware Leader / 有界 wave scheduling | M5 | partial | ADR 0137 在保留 `SELECT_NODE`/`max_parallel=1` compatibility 的同时，为 zero-tool Leader 增加 typed `SELECT_NODES`。Selection 必须非空、无重复、按 canonical `(ordinal,node_id)` 排序，并受不可变 `max_parallel` 与当前 durable free capacity 限制；non-canonical、stale、overflow、unknown 以及 running-node `FINALIZE` decision fail closed。Evidence 包含 parent session、graph/node generation、running ID、available capacity、READY ID、dependencies、有界脱敏 outcome 与 completed/failed/cancelled/skipped/indeterminate bucket。Task DAG 继续是 capacity 与 execution authority：SQLite graph/node generation CAS 只 claim selected node，绝不替换成未选择的 READY node，并在结构化 wave 中运行独立 Writable service。Schema 24 migration 保留已填充的 schema-23 Leader row，并增加 parent/selection/generation projection。Focused 与 production-shaped tests 覆盖 durable decision reuse、partial-claim recovery、spawned-process death、provider no-replay 与 concurrent-controller fence。Dynamic graph、replan、retry、merge、rollback、cleanup 与 public UI/ACP 仍不属于本切片；bounded Swarm composition 由 ADR 0140 单独规定，Ultracode entry 由 ADR 0141 规定但不属于本 wave service。详见 [ADR 0137](adr/0137-parallel-aware-leader-bounded-wave-scheduling.md) |
| 有界模型生成 DAG planning | M5 | partial | ADR 0138 增加一个显式内部 zero-tool Planner，把来自实际 parent 的一个 objective 转成一个有界、不可变的 Task DAG proposal。Planner binding 没有 local/provider-hosted tool、filesystem、Bash、terminal、network、MCP、LSP、Worktree、Checkpoint、worker 或 background authority；只接收一个有界、确定性、脱敏的 USER/可见 ASSISTANT context envelope。严格 JSON 只允许 `nodes`、`max_parallel` 和有界 `reason`；node 只包含 `id`、`prompt`、`depends_on`，遵守 declaration order/canonical dependency order，并保留冻结的 Task DAG node、edge、dependency、prompt 与 parallelism bounds。TaskDagApplicationService 继续是 validation/publication authority。Schema 25 新增 insert-only planning attempt/proposal，绑定实际 parent/planner session 与 turn identity、objective/context/proposal fingerprint 和预分配 intended DAG ID；当前 schema 保留这些 projection。Claim/fence/model-commit/proposal/DAG publication lifecycle 与 spawned-process crash recovery 不会 replay 已观察到的 provider output，不会改变 immutable proposal，也不会创建第二个 DAG；live/unproven owner 不会被抢占。production-shaped acceptance 跑通 Planner → 真实 parallel-aware Leader → 真实隔离 Writable worker，并验证 A→B/C→D、B/C overlap、D 顺序、不同 managed worktree 及 dirty parent checkout 不变。Bounded failed-DAG revision/replan 由独立的 [ADR 0139](adr/0139-bounded-dag-revision-replan.md) 实现；internal bounded Swarm composition 由 [ADR 0140](adr/0140-bounded-agent-swarm-durable-orchestration-run.md) 单独实现。recursive replan、retry、dynamic/无界 dataflow、自动委派、merge/copy-back、rollback、cleanup、public UI/ACP 与 recursive Ultracode 仍不支持；Ultracode 入口由 ADR 0141 单独规定。详见 [ADR 0138](adr/0138-bounded-model-generated-dag-planning.md) |
| 有界 DAG revision / replan | M5 | partial | ADR 0139 增加一个显式内部 revision：把一个 quiescent `FAILED` source DAG 转成一个不可变 successor DAG。Source 永不修改，且不能有 `RUNNING` 或未解决的 `INDETERMINATE` node；`MAX_DAG_REPLAN_DEPTH=1` 只允许一个 successor。有界、确定性、脱敏 evidence envelope 只传递精确 source identity/generation、canonical node state/dependency、有界 completed-result projection、typed failure summary 与安全 metadata；transcript、tool data、log、environment、secret、workspace bytes、checkpoint、diff、path 与 authority instruction 都排除。专用 Planner binding 为 `max_steps=1` 且没有 tool、filesystem、Bash、terminal、network、MCP、LSP、Worktree、Checkpoint、worker 或 background authority，只返回既有 typed `ModelDagProposal`。Schema 26 以 insert-only 方式保存 exact source/evidence/proposal/successor identity；当前 schema 保留这些 projection。Durable claim/fence/model-commit/proposal/successor/completion lifecycle、source snapshot fence、exact duplicate recovery、one-winner race、spawned-process crash recovery 与 no-provider-replay 由 fixture-provider tests 覆盖；provider-turn-evidence boundary 转为 explicit `INDETERMINATE`。Publication 与后续 Leader/Writable execution 仍由既有 service 拥有。Recursive/multi-level replan、automatic retry、source resurrection、merge/copy-back、rollback、cleanup、public CLI/TUI/ACP orchestration、Ultracode entry invocation 与 live/paid Provider validation 仍不支持于本 replan slice；bounded Swarm composition 由 [ADR 0140](adr/0140-bounded-agent-swarm-durable-orchestration-run.md) 单独规定。详见 [ADR 0139](adr/0139-bounded-dag-revision-replan.md) |
| 有界 Agent Swarm / 持久化编排运行 | M5 | partial | ADR 0140 在既有 model-generated Planner、parallel-aware Leader、Task DAG、Writable Subagent、Parent Context Relay、predecessor-result Relay、Worktree、Checkpoint、worker-scoped LSP 与 bounded Replan service 之上增加内部有界组合。它只拥有一个绑定 parent 的 Swarm identity、durable lifecycle、精确 DAG lineage、有界 terminal response 与单 controller ownership fence。production-shaped 正常路径为 Planner → A/B,C/D DAG → 真实 Leader `SELECT_NODES` 使 B/C 重叠 → 独立 Writable worker/worktree/checkpoint/worker-scoped LSP → durable predecessor-result fan-in → B+C 后执行 D → Leader `FINALIZE` → durable result。失败路径只接受一个没有 `INDETERMINATE`、quiescent 且 terminal 的 `FAILED` source，通过既有 Replan service 在 `MAX_DAG_REPLAN_DEPTH=1` 下创建一个 immutable successor，不 retry、不复活 source node、不创建第二个 successor。Schema 27 增加带 parent/planning/DAG lineage、lifecycle、脱敏 result、foreign-key `RESTRICT`、process-liveness ownership 与 generation CAS 的 insert-once `orchestration_swarm_runs`。代表性的 `multiprocessing`-spawn matrix 证明四个 fresh-process handoff：Swarm `PLANNED` 前的 Planner state、`FINALIZING` 前的 lower DAG 终态、current-DAG 切换前的 Replan successor（source 保持不可变），以及 `COMPLETED` 前的 `FINALIZING` result；它校验精确 identity/resource count 且不 replay Provider。组合层 lower `INDETERMINATE` result 为终态且不 Replan；这些是代表性有界证据，不覆盖任意 kill point 或 live/paid provider。Fresh controller 复用 durable terminal result；live-stale loser 不执行 Provider、DAG、worker 或 result mutation，provider-turn uncertainty/cancellation 为 terminal `INDETERMINATE`。本能力仅内部提供：recursive Swarm、无界 agent/queue、generic retry、共享 worktree、merge/copy-back/cherry-pick/patch adoption、public CLI/TUI/ACP orchestration、remote/cloud execution、marketplace integration 与新的 Checkpoint/Rollback 实现仍不支持。Ultracode 入口由 ADR 0141 单独规定。详见 [ADR 0140](adr/0140-bounded-agent-swarm-durable-orchestration-run.md) |
| Automatic Ultracode delegation / durable branch entry | M5 | partial | ADR 0141 增加显式 `ULTRACODE` 应用入口。当前确定性有界策略是针对并行/拆分、跨文件和研究措辞的固定 marker heuristic，不是语义或 model classifier；它会准确持久化选择一条 `MAIN_MAX` 路径（复用既有普通 ConversationRunner）或一条 `BOUNDED_SWARM` 路径（复用既有 Agent Swarm）。交互式 TUI 只绑定一个 dormant delegate，并通过同一个长生命周期 turn service 接受运行时 `max` ↔ `ultracode` 切换；普通 effort 永远不委派。schema-28 insert-once identity、精确下游 ID、parent-visible exactly-once result finalization、provider-wire neutrality 与 no fallback 继续受保护。原始 fresh-process A–E matrix 是直接 durable-state 证据；另有两个 fresh-composition process-death acceptance，分别覆盖 parent result 已持久化后的 MAIN_MAX 与既有 Swarm 完成后的 BOUNDED_SWARM，并验证精确 identity、单次 Provider/no-replay 以及 Planner/Leader/Worker/lease/worktree/checkpoint/DAG resource count 不增加。Stacked automatic result-adoption integration 由 ADR 0144 单独实现；普通 effort、递归编排、retry、merge/copy-back、remote execution 与 public dashboard 仍不属于本切片。详见 [ADR 0141](adr/0141-automatic-ultracode-delegation.md) |
| 有范围的会话权限授予 / 减少审批疲劳 | M5 | partial | ADR 0142 保留精确操作 `ALLOW_SESSION`，并为普通主工作区 `search_replace`/`apply_patch` 编辑以及保守的 `test`、`static_check`、`git_read` 命令族增加仅内存、由运行时生成的有类型候选。候选只能在规范目标计划或严格 Bash 分类之后生成，绑定 session identity 与工作区规范根，绝不改变 authority、sandbox、capability、network、MCP 或策略决定。显式 deny/ask、mode、headless 请求、受保护/非主工作区/删除/移动目标、高风险命令、ACP 远程输入以及 model/planner/worker 输入都不能创建宽范围 grant。排队等价请求会重新检查 grant；仅允许一次、拒绝和取消不会替等待者授权。TUI 保持拒绝为默认焦点，只展示有界的 root/family metadata；ACP 保留精确操作选项。不改变 schema 或持久化规则。详见 [ADR 0142](adr/0142-scoped-session-permission-grants.md) |
| 有界持久化结果采纳核心 | M5 | partial | ADR 0143 增加显式内部 application service，只消费 completed Swarm/Task DAG projection、preserved Writable lease、managed READY Worktree、READY baseline Checkpoint 与 live worker projection，生成由 application 拥有的不可变 `ResultAdoptionPlan`。它重新校验精确 parent `ConversationBinding` session/root/repository/HEAD 与 source identity；worker response text、Leader/Relay text、`git diff`、model path 和调用方 manifest 都不是 authority。保守 three-way plan 支持 regular-file `CREATE`/`UPDATE`/`DELETE`，保留 parent 无关 dirty file，在 `APPLYING` 前拒绝 parent 同路径冲突，在 plan 发布前拒绝 worker overlap，并对 link、special file、mode-only change、受保护状态、credential/key path 与 root 外 target fail closed。Schema 29 增加 insert-only adoption 与逐 target CAS projection，生命周期为 `CLAIMED -> VERIFIED -> APPLYING -> VERIFYING -> COMPLETED`，并支持 `CONFLICT`/`FAILED`/`INDETERMINATE`；当前 schema 保留这些 projection。Expected/desired image recovery 不覆盖第三种 image，completed adoption 幂等。Parent write 复用 canonical permission/workspace/sandbox/exact-file pipeline；`DELETE` 不获得 broad `WORKSPACE_EDITS` candidate。Production-shaped 临时 Git tests 覆盖 A/B/C/D fresh-process recovery、A/C adoption、B/U/HEAD 保留、worker evidence 保留、`INDETERMINATE` 时第三方 bytes 保留、completed 重入时零新 mutation/resource row、显式 DELETE approval 且没有 broad candidate、conflict、overlap，以及 schema 28→29 migration/幂等初始化。Automatic Ultracode wiring 只由独立 ADR 0144 composition slice 提供；model merge、cleanup、rollback、commit/push、remote execution、TUI/ACP 暴露与通用 merge/copy-back engine 仍不属于本核心。详见 [ADR 0143](adr/0143-bounded-durable-result-adoption.md) |
| Automatic Ultracode 结果采纳集成 | M5 | partial | ADR 0144 只把显式 `ULTRACODE` 用户回合接入既有 bounded Swarm 与内部 Result Adoption service。`MAIN_MAX` 执行零次 adoption activity。Legacy `BOUNDED_SWARM` 把精确 canonical terminal `AgentSwarmResult` 与实际 parent `ConversationBinding` 传给 Result Adoption，随后提交有界 external result。结构化 `BOUNDED_SWARM` 使用同一个 lower path，但在 adoption 后启动真实 parent AgentRuntime；parent 拥有 verification、mutation freshness、finalization 与 committed response，adoption conflict/indeterminate 则通过 parent-owned deterministic fallback。既有 `ULTRACODE_DELEGATION_PROGRESS` 暴露安全 adoption stage。Fresh-process recovery 证明已完成 lower work 不会重放，不会产生重复 parent attempt 或 assistant item，并保持 Worktree/Checkpoint/lease resource 与 committed-parent replay。Semantic merge、cleanup、rollback、commit/push、remote execution、persistent permission grant 与 public UI/ACP result-adoption control 仍不支持。详见 [ADR 0144](adr/0144-automatic-ultracode-result-adoption-integration.md) 与 [ADR 0157](adr/0157-ultracode-bounded-swarm-parent-verification.md) |
| Shell 方言与可恢复命令契约 | R0.5a | compatible | 公共 `bash` 标识保持不变。共享启动器事实明确 POSIX 系统 Shell 路径使用 `/bin/sh` 语义而不是 Bash（需要 Bash 专属行为时，若 Bash 可用则显式调用 `bash -c`），Windows 使用受信任的 `cmd.exe /c`；前台和受管后台路径共享同一方言。非零前台/后台结果保留有界 stderr 与规范退出状态，可在同一模型回合中恢复，且不会自动重试有副作用命令。通用指引要求独立质量检查分开执行，并将长任务导向受管 Bash → `task_output` → `wait_tasks`，避免 `pytest ... | tail` 等隐藏输出的管道；权限、沙箱和进程树所有权保持不变 |
| 语音、图像、视频和网页工具 | M5 | planned | 需要由提供商支持的适配器 |
| Leader、更丰富 relay 与 Computer Hub | M5 | planned | 本地组件与外部服务边界 |

## ACP 私有扩展

私有命名空间扩展 `_neuro-code/session/artifacts` 已实现,可有界地列出/读取与会话关联的本地脱敏工具输出
artifact.它不会作为标准 ACP capability 宣告;其他有界私有扩展由各自 owner ADR 说明,超出已接受扩展
边界的方法仍不支持.
