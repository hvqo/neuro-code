# Compatibility matrix

[简体中文](../zh-CN/compatibility-matrix.md) · **English**

Statuses: `unassessed`, `planned`, `partial`, `compatible`, or
`intentionally-different`. “Evidence” points into the pinned Rust repository.

The current production Windows sandbox source verification is CI run
`32374860136` at head `1175fa046cde263a9a1c9afbb1a32b7f6ed34914`; it completed
all 23 jobs. The final branch update is evidence-only documentation.

| Capability | Target | Status | Evidence / notes |
|---|---|---|---|
| Semantic text accents | TUI | intentionally-different | Theme-specific semantic colors for Markdown, syntax, tools, and diffs across 13 themes. Rendering and static checks only; no test suite. |
| Settings visual hierarchy | TUI | intentionally-different | Appearance-first navigation on wide screens; grouped surfaces, row separators, and effective-value forms. Targeted Settings UI regression tests and the full repository gate cover rendering and behavior. |
| Scoped interactive preferences | TUI | intentionally-different | Agent preferences support user and workspace scopes, effective-value provenance, CLI precedence, startup snapshot inspection, and additive optional fields in the existing version-1 JSON format. Targeted persistence and Settings tests cover inheritance and scope overrides. |
| Persistent TUI agent settings | TUI | intentionally-different | Runtime-affecting preferences rebuild the composition and resume the same Session; interface-only preferences apply live. Explicit CLI precedence, permissions, and sandbox ownership are preserved. |
| TUI settings navigation | TUI | intentionally-different | Seven groups, cross-category search, effective values with Default/User/Project/CLI sources, and a compact single-column layout. Advanced retains raw controls and existing persistence boundaries. |
| Capability-driven Web Search settings | TUI | intentionally-different | Search offers Off/Auto/Custom. Custom profiles come only from the active runtime projection of credentialed profiles with a trusted executable Search backend; unavailable choices fail closed with a typed reason and optional Manage Providers entry. Runtime reload preserves the current Session and uses existing route/tool composition. See [ADR 0175](adr/0175-effective-settings-and-web-capability-ux-v2.md). |
| Ultracode parent-workspace freshness projection | M5 | compatible | VF-4b derives `ResultAdoptionRecord.parent_workspace_changed` from durable target lifecycle without a schema column. A target that reached `APPLIED` retains that fact through the exact post-apply final-verification `INDETERMINATE` transition; pre-apply uncertainty and no-target outcomes remain false, while partial conflict/indeterminate outcomes retain the fact when another target reached `APPLIED`. One stable adoption identity is one future parent verification-mutation boundary, including recovery re-entry. Worker verification evidence is not imported. See [ADR 0156](adr/0156-ultracode-parent-workspace-freshness.md) |
| Ultracode objective-size boundary | M5 | compatible | The single domain-owned `MAX_SWARM_OBJECTIVE_BYTES` contract is 4 KiB measured as UTF-8 bytes. Agent Swarm request validation and the Ultracode marker policy share it; an oversized marker-bearing prompt selects `MAIN_MAX` before the durable branch claim. This is pre-decision routing, not fallback, and recovery does not reclassify an existing durable decision. See [ADR 0141](adr/0141-automatic-ultracode-delegation.md) |
| Ultracode MAIN_MAX verification snapshot | M5 | compatible | Structured verification is supported on both the `MAIN_MAX` and `BOUNDED_SWARM` parent paths. A fresh absent request is resolved once by `NormalTurnRequirementsPolicy`; explicit non-empty and empty snapshots are preserved exactly in `TurnInput` and the immutable Ultracode execution identity. Schema 30 adds two nullable snapshot columns; NULL/NULL remains legacy mode, while partial, malformed, oversized, or fingerprint-mismatched structured rows fail closed. Recovery replays an exact committed parent result without provider or finalizer rerun. Structured `BOUNDED_SWARM` runs the lower Swarm and adoption first, then invokes the normal parent runtime with the exact snapshot; legacy NULL rows retain the external-result path. See [ADR 0155](adr/0155-ultracode-main-max-verification-snapshot.md) and [ADR 0157](adr/0157-ultracode-bounded-swarm-parent-verification.md) |
| Package and CLI composition root | M1 | partial | The normal package includes Textual and publishes equivalent `neuro` and `neuro-code` console scripts; `neuro code` is the explicit TUI alias, all three use the launch directory as the workspace, and globally installed tools require no per-use venv activation. CLI, TUI, and ACP share an interface-neutral `ApplicationComposition`; standalone native installers remain pending |
| Preview package/version boundary | M1 | partial | `src/neuro_code/_version.py` is the single version authority for `neuro_code.__version__`, Hatchling metadata, CLI output, inspection, and ACP negotiation. R1 targets PEP 440 `0.1.0a1`; `scripts/release_candidate.py` builds and verifies wheel/sdist artifacts, clean installs, content safety, secret safety, and SHA-256 provenance without public publication. |
| Effective configuration inspection | M1 | partial | `neuro-code inspect`; secret-redacted Python view implemented |
| Provider service catalog and preset ownership | M3/P3A/P3B | partial | `ProviderServiceDescriptor` / `ProviderServiceCatalog` describe the thirteen current services, including Ark, Qianfan, Bailian, and TokenHub, preserve existing UI keys and profile matching, and are consumed by an injectable TUI catalog. Service metadata does not own credentials or runtime execution; adding a future service is descriptor-plus-tests work. P3B keeps service, publisher, protocol, model, endpoint, and billing/workspace metadata separate. See [ADR 0117](adr/0117-provider-service-capability-routing-foundation.md), [ADR 0122](adr/0122-china-direct-provider-compatibility-expansion.md), and [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| Canonical model and hosted-tool capabilities | M3/P3A/P3B | partial | `ModelCapabilitySet` provides supported/unsupported/unknown status. Runtime preserves upstream service → protocol → model facts, meets them with trusted adapter implementation capabilities, and applies only explicit configuration restrictions; metadata cannot elevate an unsupported or unknown adapter. P3B adds model-specific Chat/Responses/Anthropic matrices, optional publisher hints, and TokenHub native/compatibility-converted/unsupported Responses metadata without adding hosted vendor tools. Existing xAI builtin tools remain unchanged; standard OpenAI Responses exposes hosted Web Search only when `builtin_tools = ["web_search"]` is explicit. Gemini Interactions adds protocol/model-specific Google Search, URL Context, and `MIXED_HOSTED_AND_CLIENT_TOOLS` facts; unknown Gemini models remain fail-closed. Kimi, GLM, MiniMax, Ark, Qianfan, Bailian, and TokenHub hosted web status remains unknown or unsupported unless a separate trusted implementation exists. See [ADR 0117](adr/0117-provider-service-capability-routing-foundation.md), [ADR 0118](adr/0118-hosted-web-search-execution-and-sidecar-routing.md), [ADR 0120](adr/0120-gemini-interactions-and-hosted-web-tools.md), [ADR 0122](adr/0122-china-direct-provider-compatibility-expansion.md), and [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| Runtime roles and model routes | M3/M5 | partial | `RuntimeRole.MAIN` and optional `WEB_SEARCH` routes are validated, redacted, and executed independently; legacy `[routing] default`/`fallbacks` project to MAIN while role-specific fallbacks remain isolated. `WebSearchMode` resolves disabled/auto/inline/sidecar fail-closed, while `WebFetchMode` resolves disabled/auto/local/inline without a sidecar: hosted fetch is retained only for explicit MAIN support and local fetch is registered only after the selected mode permits it. See [ADR 0118](adr/0118-hosted-web-search-execution-and-sidecar-routing.md) and [ADR 0121](adr/0121-local-safe-web-fetch.md) |
| Hosted Web Search and Web Fetch vertical slice | P1/P1.2/P2/P3A | partial | Canonical bounded request/source/citation/result contracts, `WebSearchService`, independent WEB_SEARCH fallback, OpenAI standard Responses and xAI sidecars, Anthropic Messages `web_search_20260318`/`web_fetch_20260318` server tools, and Gemini Interactions Google Search/URL Context hosted capabilities are implemented. Gemini uses stable stateless Interactions requests, typed step lifecycle, bounded native replay, structured `url_citation` extraction, forced Google Search sidecars, and capability-aware inline versus sidecar routing; DeepSeek/OpenAI-compatible MAIN → local `web_search` → search route → canonical `ToolResult` remains intact. `AUTO` now deterministically discovers only a configured profile with a trusted, executable hosted-search backend and available credentials when no explicit sidecar route is configured; effective binding status is inspectable without exposing endpoints or secrets. Kimi, GLM, and MiniMax MAIN profiles use that same local sidecar path and P2 local fetch; no native hosted web capability is claimed for them. Portable tests cover source bounds, cancellation, usage isolation, redaction, untrusted evidence boundaries, and all three China MAIN compositions. Live Gemini/China smoke is opt-in and credential-gated. See [ADR 0118](adr/0118-hosted-web-search-execution-and-sidecar-routing.md), [ADR 0119](adr/0119-anthropic-hosted-web-search-and-fetch.md), [ADR 0120](adr/0120-gemini-interactions-and-hosted-web-tools.md), [ADR 0121](adr/0121-local-safe-web-fetch.md), [ADR 0122](adr/0122-china-direct-provider-compatibility-expansion.md), and [ADR 0174](adr/0174-agent-reliability-and-web-search-recovery.md) |
| Web Search routing and provider adapters | Web Search | implemented | A trusted capability registry resolves MAIN native Search, protocol-specific hosted adapters, the DeepSeek Anthropic-compatible Search adapter on the exact official route, and user-configured Brave as final fallback. DeepSeek reuses its configured key only for the fixed official endpoint; custom bases and gateways are rejected. Its adapter sends an explicit bounded search instruction and enforces domain filters on returned hosts. OpenAI compatibility alone does not confer Search capability, including for Qwen. Unsupported/unavailable routes may fall through; authentication, rate-limit, invalid-request, and malformed-response errors surface directly. `DID_NOT_SEARCH` is request-scoped and does not quarantine the route; each new request retries candidates in configured priority order. Confirmed route-wide unsupported/unavailable errors remain suppressed for the binding. The canonical local tool is registered only when an executable route exists and policy allows it. Results remain bounded, redacted, and untrusted evidence. Live paid API calls are outside the default gate. See ADR 0177 (adr/0177-independent-search-api-backend.md). |
| Agent external-evidence escalation and loop recovery | M3 | partial | Stable Runtime guidance checks workspace evidence first, then uses the model-visible `web_search` tool for required public facts when available; otherwise it reports the blocker and keeps external claims unverified without silently substituting shell scraping. Repeated action/observation, error, periodic-cycle, and no-progress detectors issue a request-only replan before terminal `STUCK`; bounded per-turn state retains only typed reason, hashed signature, cycle period, and tool boundary. Meaningful evidence, workspace, plan, verification, or external-state progress clears the pressure. Typed completion diagnostics and `/status` show safe reason/capability projections without arguments, result bodies, secrets, endpoints, URLs, or fingerprints. Existing execution budgets, batch ordering, permissions, sandbox, and canonical history remain authoritative. See [ADR 0174](adr/0174-agent-reliability-and-web-search-recovery.md) |
| Local Safe Web Fetch | P2 | partial | `WebFetchRequest`/`WebFetchResult`, `WebFetchService`, public-only `aiohttp` transport with pinned A/AAAA validation, manual bounded redirects, HTTPS downgrade rejection, direct/no-proxy/no-cookie GETs, decompressed body limits, text MIME allowlist, charset fallback, non-executing HTML-to-Markdown extraction, secret-in-URL fail-closed handling, redacted `[UNTRUSTED WEB CONTENT]` tool results, provenance, cancellation, and mode-gated composition ownership are implemented. Browser/JS/Playwright/PDF/crawling/auth/cookies/local-network/file/FTP/WebSocket/cache and new providers remain unsupported. See [ADR 0121](adr/0121-local-safe-web-fetch.md) |
| Provider model-discovery strategy | M3/P3A/P3B | partial | Catalog discovery accepts explicit OpenAI-compatible, Anthropic, Gemini, static, or manual-only strategies with protocol-derived behavior retained as a compatibility fallback. Kimi, MiniMax, Qianfan OpenAI-compatible routes, Bailian OpenAI-compatible routes, and TokenHub use bounded inference model paths; GLM and Ark use bounded official/static evidence where a stable inference catalog was not accepted; Qianfan and Bailian Anthropic routes are manual-only because their official compatibility pages do not document a safe `/v1/models` endpoint. Remote availability never promotes model capability or protocol status. Manual model entry remains available, and the TUI uses a credential-free, atomically written bounded catalog cache with classified offline fallback. See [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| Named provider profiles and selection | M2/M3/P3A/P3B | partial | Four explicit wire protocols, Auto/Recommended UI selection that resolves to a concrete protocol, default/one-shot selection, legacy TOML compatibility, redacted `providers list/inspect`, first-run TUI setup, a category-first Settings flow, multi-profile create/edit/save-and-use/confirmed deletion, explicit OpenAI Responses/compatible Chat/DeepSeek/Kimi/GLM/MiniMax/Ark/Qianfan/Bailian/TokenHub presets, service → endpoint variant → protocol → model selection, model-specific protocol validation, user-triggered connection testing with a bounded in-memory remote model picker and manual fallback, and persisted managed defaults through `/provider`, `/model`, and Ctrl+P are implemented. Platform credentials remain separate from normal settings and inherit the existing environment/direct/explicit proxy policy. All P3B routes reuse `OpenAICompatibleProvider`, `OpenAIResponsesProvider`, or `AnthropicProvider`; no four-platform runtime adapters are added. Endpoint/service identity participates in native context affinity, including same-publisher routes. Hosted platform tools remain unimplemented; provider-neutral Web Search and P2 local safe Web Fetch remain unchanged. See [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| Typed provider projection boundary | M3 | compatible | `ProviderOption` and `ProviderSelectionResult` are owned by `neuro_code.application.providers.contracts`; Provider application services, bootstrap, and TUI use the canonical seam while profile/session and legacy runtime imports retain identity-preserving compatibility re-exports. Provider lifecycle and selection behavior are unchanged |
| CC Switch compatibility | M2 | partial | Read-only `NEURO_CODE_CC_SWITCH_CONFIG` mapping, three backend formats, loopback `PROXY_MANAGED`, env references, inline-key rejection, and native project override implemented; CC Switch database/process control and its internal failover remain excluded |
| Safe provider failover | M2 | partial | Ordered lazy fallback profiles, first-event commit boundary, monotonic per-run selection, audited failures/selections, `--no-failover`, aggregate errors, opaque-session origin protection, bounded pre-output retry, cooldown circuit breaking, and non-secret in-memory health snapshots are implemented. Persistent health across process restarts remains intentionally absent |
| Typed provider failure taxonomy | M2 | partial | `ProviderFailure` facts classify authentication, authorization, rate limit, invalid request, model, context, server, timeout, network, protocol, and unknown failures with bounded redacted detail, status, phase, provider/model identity, bounded `Retry-After`, and evidence origin. The generic HTTP boundary is conservative: 404 is not asserted to be a missing model, unstructured 429 is not automatically retryable, and 413 is not asserted to be context overflow. Five protocol-owned offline fixture sets cover documented OpenAI-compatible/Responses, Anthropic, Gemini Generate Content, and Gemini Interactions envelopes; Anthropic `billing_error` remains authorization, Anthropic `rate_limit_error` is rate limit, and Gemini `RESOURCE_EXHAUSTED` is the documented 429 rate-limit fact across RPM/TPM/RPD/spend dimensions. `ProviderFailurePolicy` keeps retry, circuit, and pre-output failover independent; local runtime failures do not fail over, circuit-ineligible facts do not poison transient health, and output commits disable replay. This is not full provider compatibility and no live/paid validation ran. See [ADR 0126](adr/0126-provider-typed-failure-taxonomy.md) |
| Provider HTTP proxy policy | M2/M3 | partial | Per-profile environment/direct/explicit-env modes, TUI editing and pre-save validation, in-TUI recovery from a failing managed default during startup, uniform four-adapter HTTPX options, redacted inspection/errors, strict ambiguous-SOCKS diagnostics, and a `socks` installation extra are implemented. Explicit routing was manually verified against DeepSeek while an invalid inherited `ALL_PROXY` remained present; PAC, multi-proxy mounts, and default-bundled SOCKS support remain pending |
| Headless single prompt | M2 | partial | Streaming text/tool loop, plain/JSON/JSONL output implemented |
| Turn workspace checkpoint and undo | M2 | partial | B1 protects the latest eligible mutating normal turn's Git-visible source-checkout projection through a typed `SourceWorkspaceCheckpointGrant` and the existing checkpoint service. One checkpoint is durably associated after approval and before the first bounded mutation; later eligible mutations reuse it, while ignored/unbounded/unsupported mutations invalidate the guarantee before execution. `/undo` and `sessions undo <SESSION_ID>` are idle-only user actions with latest-only `AVAILABLE`/`UNAVAILABLE`/`ROLLED_BACK` state, restart proof, exact index/file restoration, and one handoff to the existing verification generation. Managed-worktree proof is unchanged; ignored files, external side effects, non-Git workspaces, process monitoring, history rollback, ACP/model undo, and whole-filesystem restoration remain unsupported. See [ADR 0158](adr/0158-turn-workspace-checkpoint-undo.md) |
| Read-only Git change inspection | M2 | partial | B2 provides one bounded typed projection for repository identity, HEAD, branch/detached state, upstream counters, porcelain-v2 status metadata, and separate staged (`HEAD -> index`) and unstaged (`index -> working tree`) diffs. `git_inspect` and CLI `inspect git` share the application service and hardened fixed-command adapter; output is bounded/redacted, binary and untracked state are metadata-only, and malformed or inconsistent snapshots fail closed. No workspace/index/ref/config/history/checkpoint/session/verification mutation, schema, TUI/ACP surface, automatic untracked-content discovery, recursive submodule inspection, or commit/rollback workflow is added. See [ADR 0159](adr/0159-read-only-git-change-inspection.md) |
| Durable history addressability and bounded rehydration | CM1 | compatible | `SessionItemQueryService` derives bounded `list`/`search`/`read` projections from the canonical ordered durable item sequence. Opaque session-scoped `shr1_` references validate ordinal and item fingerprint; `session_history` is a read-only runtime-bound model tool with no arbitrary session ID. Hidden reasoning/native/synthetic/internal content and mutations remain excluded. No second history store, Working Set, cross-session browsing, or new CLI/TUI/ACP surface is added; CM3a rollover uses this existing history source on demand. See [ADR 0160](adr/0160-durable-history-addressability-and-rehydration.md) |
| Structured session Working Set | CM2 | compatible | `WorkingSetSnapshot` is a bounded six-section durable task-state projection with immutable typed entries, optional CM1 source references, explicit model/history provenance, redaction, and revision-CAS replacement. One SQLite row is restored across restart; legacy sessions resolve empty, malformed rows fail closed, and fork/subagent sessions do not inherit state. Runtime context is a refreshed non-durable synthetic projection appended after conversation as volatile tail; `session_working_set` can only read/update the runtime-bound session and cannot select or browse another session. Working Set is not canonical history, a compaction summary, or cross-session memory. See [ADR 0161](adr/0161-structured-session-working-set.md) |
| Fresh active-context rollover | CM3a | partial | The normal Agent exposes an exclusive, no-argument, runtime-bound `new_context` control. A durable monotonic `context_generation` marker plus exclusive `context_generation_start_index` boundary (schema 33) keeps the same session/history and lets recovery rebuild a fresh active projection from the system prefix, current user input, refreshed instructions/skills, the refreshed Project Memory snapshot, current Working Set tail, and later runtime/model/tool items. Older canonical items remain available through CM1; synthetic rollover context is not persisted. Provider affinity/failover, permissions, verification, workspace state, finalization, and existing safe-boundary compaction remain unchanged. Child/subagent bindings do not receive the control; an automatic policy is specified separately by [ADR 0163](adr/0163-automatic-fresh-context-rollover.md). See [ADR 0162](adr/0162-fresh-context-rollover.md) |
| Automatic fresh-context rollover | CM3b | partial | The normal finalizing Agent keeps `ContextPreflight` authoritative: `SAFE` proceeds, `UNKNOWN` keeps the existing behavior, and an irreducible `BLOCKED` request stops without compaction or rollover. A reducible `COMPACTION_REQUIRED` request receives at most one existing safe-boundary automatic compaction; after rebuild and re-preflight, a fresh projection is previewed with the exact current user, rebuilt Working Set/instructions, frozen tools, provider identity, output reserve, and safety margin. Only a `SAFE` preview before the first real provider request can commit one automatic durable CM3a rollover; `BLOCKED`/`UNKNOWN` previews do not mutate or call the provider, and mid-turn pressure cannot discard uncommitted tool/native evidence. The request is rebuilt and defensively preflighted once more, then either reaches the provider or takes the existing deterministic budget block. The same session, canonical history, CM2 Working Set, CM1 on-demand recovery, provider-native boundary/affinity, failover origin, permission, workspace, verification, finalization, and pending-turn recovery contracts remain in force; no new counter, compaction metadata, schema, threshold, or automatic subagent policy is added. See [ADR 0163](adr/0163-automatic-fresh-context-rollover.md) |
| Model-facing tool-result guard | CM4a | partial | `ToolExecutor` keeps the complete canonical `ToolResult` for hooks, `ToolExecutionResult`, verification, supervision, workspace/progress evidence, artifacts, and terminal events, then applies one provider-neutral `ToolResultContextProjection` only to model-facing `Role.TOOL` messages. Results within the existing `ToolContext.output_byte_limit` and the 64-KiB/16,384-estimated-token global ceiling pass through unchanged; oversized text receives a deterministic UTF-8-safe head/tail projection and bounded omission metadata. Parallel batches receive deterministic equal per-call byte shares within `min(64 KiB, output_byte_limit * call_count)` and approximate token shares from the current preflight capacity. A fuller-artifact marker is enabled only by a typed artifact returned through the runtime-owned store boundary; generic custom metadata does not qualify. Existing opaque output artifacts are reused without exposing IDs or paths, and no duplicate artifact is created. The next request continues through normal `ContextPreflight` and provider-window accounting. No LLM summary, semantic retrieval, result rewrite, new artifact store, or automatic clearing is added. See [ADR 0164](adr/0164-model-facing-tool-result-guard.md) |
| Deterministic Microcompaction V1 | CM4b | partial | Microcompaction runs only for a normal user turn with known request capacity and actionable `COMPACTION_REQUIRED` pressure, in one batch before preflight or after a tool batch. It replaces only complete, ordered, prior-turn groups with runtime-confirmed successful results; the three newest old groups, current turn, errors, media/binary, malformed groups, and unknown results remain intact. One fixed marker changes only the model-facing `ModelContext`; session history, artifacts, audit, verification, and recovery facts remain canonical and readable. The application pins exact group fingerprints per session/generation and permits another batch only on a new stable-prefix/compaction boundary or after at least eight stable items/2,048 estimated tokens of growth. Proposals must save at least 1,024 serialized item bytes and 256 estimated tokens. Final Context Preflight measures the projected request: `SAFE` skips Full Compaction and rollover; remaining pressure enters the existing paths. Body-free aggregate telemetry uses the existing preflight event. State is bounded and in-memory; after restart unknown results stay full. See [ADR 0173](adr/0173-deterministic-microcompaction-v1.md) |
| Cache integrity and prompt continuity | C0–C4 | partial | `NEURO_PROMPT_TRAJECTORY=1` opts into body-free HMAC trajectory events emitted after the final adapter request body is built and before dispatch. It records ordered message/tool/stable-prefix/request fingerprints, request source, binding generation, epoch, provider/model, counts, divergence, and append-only status; message fingerprint count and trajectory bindings are bounded, and oversized comparisons fail closed. Normal Main Agent requests are tested as append-only within an epoch. A typed, memory-only projection journal retains changed runtime, Working Set, project-instruction, and skill-catalog synthetic revisions outside canonical history. Explicit boundaries cover binding/scope, provider/model, tool schema, config, Microcompaction, Full Compaction, and Fresh Context Rollover. DeepSeek V4 tool-enabled replay retains historical `reasoning_content` and bounded exact tool-argument serialization in the adapter only; other dialects keep their prior behavior. Cache metrics use only reported Provider fields with known input semantics; no cache hit is inferred. See [ADR 0176](adr/0176-cache-integrity-and-monotonic-provider-projection.md) |
| Provisional and committed final-response contract | M5 | partial | `neuro_code.application.runtime.final_response.FinalResponseContract` remains the sole owner of provisional versus committed terminal-response semantics. `AgentRunResult` and runtime `TURN_COMPLETED` projections accept only committed responses and carry bounded response source, commit state, verification state, and workspace generation metadata without adding a schema column. VF-2b preserves ordinary no-mutation streaming, but activates a bounded per-step text buffer for mutation-bearing, verification-required, or evidence-bearing turns; tool-step text is released only as intermediate output, while a no-tool candidate is replaced by the existing evidence-aware finalizer or deterministic fallback. Candidates never enter assistant history, replay, or completion persistence, and `MODEL_OUTPUT_STARTED` remains the recovery marker. Provider, CLI/TUI/ACP, and session-schema contracts are unchanged; verification discovery, requirements, and UltraCode integration remain outside this slice |
| Structured verification requirements | M5 | partial | `neuro_code.domain.execution.verification_requirements` owns bounded immutable requirement declarations, stable `req-v1` identities, mergeable strength/provenance, and snapshot fingerprints. `neuro_code.application.runtime.verification.VerificationTracker` remains the sole mutable truth owner: explicit `covered_requirement_ids` provide exact evidence linkage, per-requirement latest facts survive diagnostic-ring eviction, global workspace-generation freshness is reused, and typed blockers produce bounded `BLOCKED` evaluation. Required failures are `FAIL`; required missing/stale/blocked facts are `INCOMPLETE`; advisory-only declarations do not claim a fully verified completion. VF-3b propagates the exact optional snapshot through `RunTurnRequest`, session bindings, `AgentRuntime`, `AgentLoopRunner`, and `TurnInput` retry/recovery JSON without adding a schema column; absent legacy fields remain compatible while malformed present snapshots fail closed. VF-4c extends the same contract to structured `BOUNDED_SWARM` by running the canonical Swarm/adoption path before the parent AgentRuntime; workers remain local and do not receive parent requirements. Requirement discovery/acquisition, dedicated test runners, UI changes, and generic verification integration remain outside these slices |
| Normal-agent verification acquisition boundary | M5 | partial | `NormalTurnRequirementsPolicy` produces one stable required `ON_WORKSPACE_MUTATION` generic requirement exactly once for a fresh normal user turn with no explicit snapshot, after UltraCode routing and before TurnInput/provider/tool execution. Explicit snapshots, explicit empty snapshots, legacy recovery, background work, subagents, and UltraCode paths retain their semantics. `resolve_verification_coverage` reuses the existing trusted Bash classifier and attaches only the exact generic requirement ID to recognized `bash:test`/`bash:static_check` commands; classification scope remains descriptive. Recognized command failure is `FAILED`; only an unambiguous denied `MODE` permission fact produces `POLICY_RESTRICTION` / `BLOCKED` in this slice. `EXPLICIT_RULE` denial is deliberately not classified because that source conflates explicit deny rules with headless ASK-to-deny and restrictive Bash conversions; no reason-string inference is used. Generic finalizer wording remains conservative. No automatic discovery, framework detection, dedicated TestRunner, UI/schema change, or UltraCode verification integration is added. See [ADR 0154](adr/0154-normal-agent-verification-acquisition-boundary.md) |
| Application-owned reasoning effort | M3 | partial | Provider-neutral `low`/`medium`/`high`/`xhigh`/`max`/`ultracode`, default `high`, request-only review guidance, typed `ModelContext` propagation, `--effort`, TUI Ctrl+E and `/effort`/`/reasoning`, requested/effective display, and atomic user-preference persistence implemented. `max` remains ordinary single-agent depth. Explicit Kimi K3 and GLM 5.3/5.2 dialects map the main-path `max` projection to their configured native `max` field; other dialects omit a native effort field while retaining guidance. An explicit `ultracode` turn enters the durable bounded delegation service, which selects exactly one `MAIN_MAX` or existing `BOUNDED_SWARM` path. See [ADR 0141](adr/0141-automatic-ultracode-delegation.md) |
| Interactive operating modes | M3/M4 | partial | Application-owned `normal`/`accept-edits`/`plan`/`auto`, `Shift+Tab` cycling, `/mode MODE` completion, runtime display, atomic preference persistence, profile/session reapplication, request-only model guidance, and deterministic permission-mode mappings implemented. Plan mode now has a bounded provider-neutral `update_plan` tool, durable session restore/fork inheritance, localized TUI `/plan DESCRIPTION` plus `/view-plan`/`/show-plan`, local `/comment-plan STEP COMMENT` feedback bound to the current plan revision, and an explicit `/execute-plan`/`/run-plan` handoff that records a durable event and an immutable plan-revision snapshot in one opaque session-task lifecycle before the canonical execution request, then moves only to `accept-edits`. Explicit `/schedule-plan`/`/queue-plan` queueing and `/run-task TASK_ID` claiming are implemented with a four-task per-session bound; queued tasks never auto-start or spawn subagents. `auto` is a labelled safe preview using accept-edits defaults unless startup explicitly authorized `--always-approve`; a model safety classifier remains pending, while explicit rules and process sandboxes always remain active |
| Turn and tool timing feedback | M3 | partial | Monotonic time-to-first-actionable-result events per model step, whole-turn completion/failure duration, and local/hosted tool duration are implemented. Calls retain stable call-ID state while consecutive activity is presented as one collapsed group with bounded aggregate summaries. Click or Enter opens a fixed-height, one-call Inline Peek; clicking the open Peek or pressing Escape collapses it, including after focus moves. Up/Down selects a call and Enter opens its Output/Input/Meta Inspector. The Inspector follows live lifecycle updates without blocking Conversation; running timers refresh each Summary group at most once and skip open Peek/Inspector layouts. Conversation never loads artifact output, while Inspector lazily uses the existing session-owned 256 KiB redacted read boundary and reports truncation. Transcript Copy is disclosure-independent. Durations are client observations rather than provider-private reasoning telemetry; durable full tool transcripts remain pending |
| Context-window budget and usage display | M3 | partial | Optional positive per-profile `context_window_tokens`, redacted selection/failover propagation, startup/resume estimation, provider-reported input-plus-output usage events, compact localized status/tooltip display, `/status` reporting, and 50%/80% warning colors are implemented. The field is local metadata and is not sent as an API parameter. C1 adds conservative normal-Agent preflight when capacity and `max_output_tokens` are known: the logical context, exact tool definitions, request metadata, output reserve, and deterministic 5% margin (128-token floor, 2,048-token cap) are estimated. CM4b first applies one cache-pinned deterministic microcompaction batch at actionable pressure, then preflights the final model-facing projection; a `SAFE` projection skips Full Compaction, while existing safe-point Full Compaction may run once before the first request if pressure remains. A still-over-capacity request stops before Provider invocation. Unknown capacity or output metadata remains `UNKNOWN` and preserves the existing Provider path; exact provider tokenization remains unavailable |
| OpenAI-compatible/xAI Chat endpoint | M2/P3A | partial | Chat Completions streaming, bounded `max_tokens`, tools, validated native user-image blocks, thinking-mode tool-roundtrip continuity, and fail-closed upstream-import visible-context replay implemented; DeepSeek V4 DSML parsing is enabled only by the explicit `openai-chat` `dialect = "deepseek-v4"` profile. Kimi, GLM, and MiniMax use the same adapter with explicit dialect mappings for current thinking/reasoning controls, tool-choice restrictions, MiniMax `max_completion_tokens`/`reasoning_split`, structured MiniMax reasoning replay, model discovery, and usage/cache fields. Recorded model-specific fixtures and opt-in live tests are present; full native structured-output parity and hosted web remain unclaimed |
| OpenAI Responses/xAI dialect | M2 | partial | Generic portable Responses subset plus optional xAI encrypted reasoning, web/X/code hosted tools, profile-affine native replay, strict legacy official-host fallback, terminal-authoritative output, SSE lifecycle normalization, status stripping, fallback repair, error redaction, explicit hosted-search tool choice, dialect-specific domain filters, and structured nested source/citation extraction are implemented. Standard OpenAI `web_search` is explicit and xAI hosted behavior remains compatible; opt-in live fixtures are present, credential-gated, and allow a model override. Stateful response IDs remain pending; bounded provider-neutral durable compaction records and automatic safe-point compaction are implemented |
| Anthropic provider | M2 | partial | Native Messages SSE, text/reasoning, tools, usage, errors, secret redaction, validated user/tool-result image blocks, conservative hosted Web Search/Web Fetch capability gating, versioned direct-caller server tools, lifecycle events, native continuation replay, bounded `pause_turn`, mixed client/server tool handling, and structured source/citation extraction implemented; live/model-specific fixtures remain opt-in |
| Gemini provider | M2 | partial | Legacy `gemini-generate-content` remains the compatibility adapter with `streamGenerateContent` SSE, text/thoughts, tools, usage, errors, thought-signature round trip, and validated user inline/File API image parts. New `gemini-interactions` owns stable `/v1/interactions` SSE, stateless `store=false` requests, thought/function/builtin steps, native affinity replay, Google Search, URL Context, and mixed hosted/client capability gates; live/model-specific fixtures are opt-in |
| Core tool registry and schemas | M2 | partial | Independent Python contracts plus capability-gated background Bash/task-output/wait-tasks/kill schemas implemented |
| Read/list/grep tools | M2 | partial | Workspace-contained UTF-8 single-file compatibility plus bounded ordered `read_files`, link-safe generated-directory-filtered `list_tree`, and deterministic multi-query `grep_many`; batch output is redacted and byte-bounded, partial read failures remain usable, and independent read-only calls may run in bounded parallel groups while side-effecting and interaction-control calls remain exclusive. Internally, read/list/glob/grep ownership is split across the canonical filesystem read, discovery, and search adapters; the public tool registry and import facade remain unchanged |
| Search/replace editing | M2 | partial | Atomic exact replacement and containment implemented |
| Bash execution and cancellation | M2/M3 | partial | Bounded foreground output, null stdin, configured provider/proxy credential stripping, timeout/cancel cleanup, POSIX process-group TERM/KILL, fail-closed Windows Job Object creation with atomic `PROC_THREAD_ATTRIBUTE_JOB_LIST` assignment/restricted standard-handle inheritance/descendant wait/whole-Job termination, conversation-scoped managed background start/snapshot/bounded single- and multi-task event waits/timeout/idempotent-kill/binding-switch/application-shutdown lifecycle, bounded model-only completion reminders with task-output/wait-tasks/kill deduplication, restart-aware per-session wake ledgers with bounded cooldown/budget and missing-task reconciliation, and automatic foreground-to-background promotion through the same process tree implemented; persisted global wake default with per-provider inherit/enabled/disabled override and session-scoped `/auto-wake on|off` precedence are implemented; explicit reviewed secret injection remains pending; bounded redacted full-output artifacts for truncated local Bash previews are implemented, while ACP client-terminal and filesystem-reader artifacts remain pending; explicit background auto-wakes may inject one redacted 2 KiB-per-task/8 KiB-per-batch output preview |
| Permission rule precedence | M2/M3 | partial | Deny/ask/allow precedence, per-segment `&&`/`||`/`;`/pipe checks, wrapper peeling, nested `bash -c`, fail-closed complex scripts, asynchronous allow-once/deny, memory-only exact-action session approval, per-ACP-session `session/request_permission` adaptation, path/operation-aware rules, and bounded atomic JSON rule persistence are implemented; local deny/workspace/sandbox decisions remain authoritative and pending tool updates precede client approval. Full rule/file-access grammar and an interactive rule editor remain pending |
| SQLite session event store | M2/M3/M4/M5 | partial | Current development schema v35 with serialized v1-v35 migrations, profile-affinity and fixed-sandbox metadata, stable titles, synchronized FTS5 documents, durable plans/tasks/turn recovery, background-wake and compaction ledgers, context-generation boundaries, result adoption, Working Set, and foreign-keyed parent-child subagent links. Internal migration paths are tested as development compatibility; no prior public Neuro Code release exists, so preview-to-preview upgrade compatibility begins only after the first published preview establishes a release baseline. Existing session list/resume/fork/export and workspace-identity behavior remains bounded; execution records, compaction items, subagent links, and writable leases remain excluded from FTS/export/import/fork copying. |
| Session titles and full-text search | M3 | partial | Deterministic ten-word titles from the first visible user prompt, upstream `generated_title` preservation, atomic manual title/FTS updates through `sessions rename` and TUI `/rename`/`/title`, title-weighted SQLite FTS5, multi-token AND-to-OR fallback, cwd filtering, pagination, matched fields, optional snippets, `sessions search`, and workspace-scoped TUI `/sessions QUERY` results implemented; system messages, private reasoning/native items, tool arguments/raw results, and image URLs are not indexed. The live debounced session-picker search and workspace-scoped ACP session listing are implemented; model-generated titles and an ACP full-text-search method remain pending |
| Typed session-turn application boundary | M3 | compatible | `RunTurnRequest`, `SessionTurnRunner`, and `SessionTurnService` are owned by `neuro_code.application.sessions.turns`; CLI, TUI, ACP, and application consumers use the canonical seam, while the package and legacy session service retain identity-preserving compatibility re-exports. Turn locking, persisted context, cancellation, event ordering, and Runtime behavior are unchanged |
| Typed session-binding application boundary | M3 | compatible | `ConversationBinding` and `ConversationRunner` are owned by `neuro_code.application.sessions.binding`; ACP, bootstrap, session application, and runtime-facing consumers use the canonical seam, while profile and legacy runtime paths retain identity-preserving compatibility re-exports. Profile-specific session selection and binding replacement, turn locking, cancellation, and Runtime behavior are unchanged |
| Typed session-selection and policy projections | M3 | compatible | `SessionOption`, `SessionSelectionResult`, `ReasoningEffortSelectionResult`, and `InteractionModeSelectionResult` are owned by `neuro_code.application.sessions.contracts`; TUI consumes the canonical projections while profile and legacy runtime paths retain identity-preserving compatibility re-exports. Selection, policy application, locking, resume, and wire behavior are unchanged |
| Typed interactive session-selection seam | M3 | compatible | `SessionSelectionController` and non-owning `SessionSelectionService` are owned by `neuro_code.application.sessions.selection`; TUI uses the facade for list/search/select/rename while `ProfileConversationController` retains locking, binding replacement, workspace checks, resume lifecycle, and the compatibility execution-record projection. Session schema, Runtime, Provider, Finalizer, and wire behavior are unchanged |
| Session projects and library management | M3 | compatible | Sessions may optionally belong to one project; the sessions database schema v35 adds the `session_projects` table, a unique normalized-name index, and a nullable `sessions.project_id` column inside the serialized v1→v35 migration chain. `SessionProject` and `normalize_project_name()` are owned by `neuro_code.domain.sessions`, while `SessionLibraryService` owns bounded project CRUD, arbitrary-session rename/delete, project assignment, and the `SessionLibraryOwner` seam for workspace listing and starting a fresh session. `ProfileConversationController.start_new_session()` reuses the existing binding-replacement path, so a new session is persisted lazily on its first turn and the previous session stays durable; deleting the open session is refused, and deleting a project detaches its sessions and purges its Project Memory. The TUI `SessionLibraryScreen` returns typed actions that the controller executes before reopening, `/sessions` opens the manager, and `/resume` keeps direct resume. Permission, sandbox, verification, supervision, Runtime, Provider, and wire behavior are unchanged. See [ADR 0165](adr/0165-session-projects-and-library-management.md) and [ADR 0172](adr/0172-project-memory-v1.md) |
| Project Memory V1 | M3 | partial | Optional sessions share bounded local memory only through their canonical `SessionProject.id`; `cwd` never creates or selects memory scope, and unassigned sessions and default subagent bindings receive none. The state-root file adapter validates UUID scope, exact body ids, no-follow reads, containment, filenames, count, and byte limits. Each project has a manifest, generated bounded `MEMORY.md` contextual-evidence index, and exact-id read-only recall tool with a stable definition that fails closed while unbound; request injection is synthetic and never durable. The application pins the index snapshot for an active context generation, so extraction writes cannot rewrite the active prefix. New sessions/resumes, project attach/move/detach, and committed Fresh Context Rollover refresh it; rename preserves it, and full compaction does not. A composition-owned extraction worker uses a per-project lifecycle lock, per-session cursor, manifest deduplication/update, explicit bounded remember/forget, tools-disabled provider request, strict source/prompt/output/event/candidate/queue/time budgets, typed observable outcomes, and shutdown cancellation. Project lifecycle and deletion semantics remain compatible with schema v35, provider affinity, permissions, and sandbox ownership. Global memory, semantic/vector/graph search, cloud sync, and a full management UI remain out of scope. See [ADR 0172](adr/0172-project-memory-v1.md) |
| Bounded user attachments | TUI/CLI | compatible | Images and text files attach to a turn through `application/sessions/attachments.py` (images ≤5 MiB each/≤10 MiB total/≤8 per turn as data-URI IMAGE parts; decodable text files ≤64 KiB each/≤128 KiB total inlined into the composed prompt). The request estimator projects media parts as bounded accounting facts plus a fixed per-image token allowance, so large attachments no longer inflate `ContextPreflight`. The TUI provides `/attach`, an attachment tray, and `Ctrl+V` clipboard-image paste via a `ClipboardImageReader` port; the CLI provides repeatable `--attach`; ACP keeps its inline-prompt contract with the shared bounds. Attachments are user input and never touch the sandbox or approvals; they are not indexed by session search. See [ADR 0167](adr/0171-bounded-user-attachments.md) |
| Typed durable session lifecycle boundary | M3 | compatible | `StartSessionRequest`, `ImportSessionRequest`, `RenameSessionRequest`, `ForkSessionRequest`, `DeleteSessionRequest`, and `SessionLifecycleService` are owned by `neuro_code.application.sessions.lifecycle`; Runtime session creation, CLI import/rename, and ACP fork/delete use the canonical requests while the legacy session service delegates with identity-preserving compatibility exports. Workspace visibility, binding replacement, turn locking, protocol cleanup, schema, Runtime execution, Provider, Finalizer, and wire behavior are unchanged |
| Typed session-task query boundary | M3 | compatible | `ListSessionTasksRequest`, `GetSessionTaskRequest`, `SessionTaskQueryController`, and `SessionTaskQueryService` are owned by `neuro_code.application.sessions.task_queries`; Runtime queued-plan lookup and `AgentConversation` task reads use the canonical seam while the legacy session service delegates with identity-preserving exports. Task writes, state transitions, locking, cancellation, Runtime, Provider, Finalizer, schema, and wire behavior are unchanged |
| Typed session-summary query boundary | M3 | compatible | `GetSessionSummaryRequest`, `SessionSummaryQueryController`, and `SessionSummaryQueryService` are owned by `neuro_code.application.sessions.summary`; resume, bootstrap configuration, ACP workspace validation, and session-scoped tool-output artifact reads use the canonical seam while the legacy session service delegates with identity-preserving exports. Lifecycle writes, event/item reads, schema, transactions, Runtime, Provider, Finalizer, and wire behavior are unchanged |
| Typed execution-record query boundary | M3 | compatible | `LoadExecutionRecordRequest`, `LoadExecutionRecordsRequest`, `SessionExecutionQueryController`, and `SessionExecutionQueryService` are owned by `neuro_code.application.sessions.execution_queries`; session catalog and conversation resume/reload reads use the canonical single/bulk seam while the legacy session service retains identity-preserving exports. Execution-record writes, schema, transactions, Runtime, Provider, Finalizer, TUI, ACP, and wire behavior are unchanged |
| Typed session-event query boundary | M3 | compatible | `LoadSessionEventsRequest`, `SessionEventQueryController`, and `SessionEventQueryService` are owned by `neuro_code.application.sessions.event_queries`; session export and session-scoped tool-output artifact reads share the canonical copied immutable mapping projection while the legacy session service retains identity-preserving exports. Event writes, decoding, schema, transactions, Runtime, Provider, Finalizer, TUI, ACP, and wire behavior are unchanged |
| Typed session-item query boundary | M3 | compatible | `LoadSessionItemsRequest`, `SessionItemQueryController`, and `SessionItemQueryService` are owned by `neuro_code.application.sessions.item_queries`; conversation resume/reload and session export share the canonical ordered tuple projection while the legacy session service retains identity-preserving exports. Item writes, plans, comments, events, schema, transactions, Runtime, Provider, Finalizer, TUI, ACP, and wire behavior are unchanged |
| Canonical application consumer imports | M3 | compatible | Bootstrap composition and the TUI import provider/workflow services from their concrete `application.providers.service` and `application.workflows.*` owners; CLI/bootstrap/ACP and CLI serialization import concrete session lifecycle/service/catalog owners. Aggregate package exports remain identity-preserving compatibility paths. This changes only imports, not service ownership, locking, persistence, Runtime, Provider, Finalizer, TUI layout, ACP wire, or workflow behavior; plan/comment/export reads remain with their existing owner because no second production consumer exists |
| Canonical tool-output artifact imports | M3 | compatible | CLI, TUI, ACP, bootstrap, ACP application orchestration, and CLI serialization import the bounded artifact application owner directly from `neuro_code.application.tools.service`; the package aggregate remains an identity-preserving compatibility path. Artifact handles, session visibility, redaction, byte limits, pruning, permissions, storage, Runtime, and protocol behavior are unchanged |
| CLI session command boundary | M3 | compatible | `neuro_code.interfaces.cli.app` canonically owns CLI parsing, top-level dispatch, presentation, and exit-code handling; `neuro_code.interfaces.cli.sessions` owns execution of the already parsed `sessions` list/search/rename/compact/artifacts/recover operations through the narrow `SessionCliServices` contract. The former root `neuro_code.cli` implementation has been removed. Existing JSON/plain output, validation/errors, artifact bounds, recovery/compaction cleanup, and `interfaces.cli.serialization` projections are unchanged. See [ADR 0152](adr/0152-cli-session-command-boundary.md) and [ADR 0153](adr/0153-architecture-completion.md) |
| Rust session import | M2 | partial | Read-only v0/v1 parsing, mixed legacy/current messages, structured images, ordered reasoning/backend-tool payloads, embedded `raw_output`/singular-reasoning recovery with backend-ID deduplication, bounded corrupt-line recovery, atomic SQLite import, built-in sandbox-profile and generated-title preservation with custom-profile rejection, prefix-safe resume, export v4, provider-native image replay, trusted-source xAI Chat replay, and strict-affinity native Responses encrypted/backend-tool replay implemented; compaction and stateful IDs remain pending |
| Full-screen/minimal TUI | M3/M4 | partial | The Textual interface implements a bounded multi-line composer, safe Markdown, stable in-place streaming with scroll-follow protection, an F8 transcript-copy view, and a conversation/composer shell that fills the terminal width with small side insets. A shared registry offers 13 palettes: Porcelain, Obsidian, system, Tokyo Night, Everforest, Ayu, two Catppuccin variants, Gruvbox, Kanagawa, Nord, Matrix and One Dark. Settings → Appearance supports scrollable preview, apply/save and cancel/restore. The composer has no extra title; the multiline editor and action row are separate. Dedicated composer/user-message fills and fine rules improve separation, with clearer placeholder, cursor and selection colors; system uses terminal foreground rules. Send reuses existing submission; Ctrl+J / F2 and a Newline button accessible with Tab then Enter insert line breaks, while modified Enter keys depend on terminal forwarding. See [TUI themes](tui-themes.md). Consecutive tool calls form one default-collapsed activity group with aggregate summaries, visible failures, elapsed time, truncated Bash intent, and click/Enter access to bounded redacted permissions, full commands, output, artifacts, and diffs. The permanent bottom area is the composer plus one label-free status row with bounded model/effort/mode and context/workspace regions; `/help` and F1 replace the shortcut row. Sparse settings and effort lists separate focus from selection, and modal dialogs use small/medium/large size classes. The existing six-level effort, four interaction modes, plan/task/background-wake workflows, provider/session setup and discovery, English/Simplified-Chinese chrome, fail-closed approvals, cancellation/retry/rewind, viewport reconciliation, durable context, lifecycle cleanup, and cross-platform headless/production-terminal tests remain implemented. Session-scoped bounded Bash output artifacts can be retrieved by the TUI on expansion; artifacts from other output sources remain pending. See [ADR 0108](adr/0108-editorial-tui-presentation.md). |
| Markdown/Mermaid/media rendering | M3/M5 | partial | Safe assistant Markdown uses an application-owned semantic theme, does not interpret model text as Rich/Textual markup, and disables hyperlink activation; headings, emphasis, code, lists, links, and tables are rendered. Tool output and diffs use literal application-styled text. Mermaid and inline media remain pending |

| Attached interactive terminal sessions | M4 | partial | Normal local CLI/TUI bindings expose `create_terminal`, `terminal_output`, `terminal_write`, `terminal_resize`, `terminal_wait`, and `terminal_kill` through the binding-owned `InteractiveTerminalManager`. Sessions are same-process, memory-only, opaque-ID handles with bounded cursor-addressed output; binding replacement/shutdown closes them and process crashes do not restore them. The TUI projects one selected session without becoming a terminal emulator. The model terminal-write path remains side-effecting and uses the existing ToolExecutor plus manager permission boundary. ACP, subagents, planners, and UltraCode workers do not receive this local family; ACP client-terminal behavior is unchanged. Attached terminals are not verification evidence |
| PTY and process-tree parity | M3/M4 | partial | Foreground/background commands share an owned `ProcessTree`; POSIX waits for a surviving process group, while Windows creates the leader atomically inside a kill-on-close Job Object, restricts inherited standard handles, waits for descendants after the leader exits, and uses handle-based whole-tree termination. A shared interactive-terminal domain/port and manager now provide bounded cursor-addressed tail output, explicit truncation, input/resize/signal/wait/close, permission/workspace/sandbox gating, cancellation-safe creation and registry shutdown. POSIX sessions own a PTY process group; production Windows sessions atomically combine ConPTY and Job-list attributes and terminate the whole Job. Portable attribute/handle/error tests, real Linux PTY tests, native process-tree tests, and opt-in three-platform CLI terminal smoke cover the boundary. ACP additionally supports capability-gated, non-interactive client foreground execution and a bounded standard direct-executable background lifecycle; it does not expose interactive PTY framing/backpressure, input, or resize |
| OS sandbox profiles | M3 | partial | Canonical `off`/`workspace`/`read-only`/`strict` configuration and CLI override, user-profile protection from project weakening, Linux child-scoped Bubblewrap and macOS child-scoped Seatbelt for Bash/background Bash/stdio MCP/LSP/enabled-profile PTY requests, private child HOME/TMP and allowlisted environment, request-scoped POSIX inode auditing, explicit close-FD boundaries, read-only edit-schema/runtime gates, profile-specific network enforcement, trusted-helper validation, fail-closed unsupported platforms, dedicated no-skip Linux and ARM64/Intel macOS security CI, native Windows Job/ConPTY CI, and session-fixed profile persistence/resume conflict detection implemented; `off` explicitly provides no OS isolation; macOS detached-descendant lifecycle remains best effort; physical SIP-enabled macOS acceptance is `DEFERRED_NO_MAC_HARDWARE` because no Mac hardware is currently available, which is not a code blocker and does not claim acceptance passed; Windows W3 provides a focused-acceptance-certified provider for enabled non-PTY `workspace` and `read-only` requests including protocol LSP children; `strict` remains fail-closed because strong read isolation is unavailable. W4 PTY/ConPTY is certified by focused native acceptance, and the W5 workload matrix in CI run `32374860136` passes all 20 measured rows through W3/W4. See [ADR 0111](adr/0111-macos-seatbelt-local-process-sandbox.md), [ADR 0112](adr/0112-windows-appcontainer-sandbox-feasibility-decision.md), [ADR 0114](adr/0114-windows-native-sandbox-runtime.md), and [ADR 0116](adr/0116-windows-developer-workload-compatibility.md) |
| Windows AppContainer sandbox feasibility | M3 | partial | The investigated classic stable unpackaged AppContainer primitives for filesystem/ACL authority, secured named-pipe IPC, runtime, and standard-user execution are viable in isolation. The current stock Git for Windows runtime remains incompatible with the complete non-admin repository workflow without protected-ancestor ACL expansion, a bundled patch, or an unsandboxed broker; those alternatives are rejected. Windows `off` remains supported through the existing Job Object/ConPTY path, which reports `STRONG_DESCENDANT_OWNERSHIP`; enabled `workspace`, `read-only`, and `strict` profiles remain unsupported / fail closed. Evidence PRs #33--#39 are unmerged. See [ADR 0112](adr/0112-windows-appcontainer-sandbox-feasibility-decision.md) |
| Windows native restricted-token and setup authority | M3 | partial | W1 adds the platform-neutral filesystem/network `LocalProcessSecurityCapabilities` axes `READ_ISOLATION`, `WRITE_ISOLATION`, and `NETWORK_ISOLATION`, with explicit `STRONG`/`LIMITED`/`UNSUPPORTED` strengths and fail-closed satisfaction. W1/W2 remain the primitive and setup authorities; their foundation actual capability constant stays `UNSUPPORTED`. The fully wired W3 non-PTY runtime provides the focused-acceptance-certified contract of limited read, strong write, and strong network; a strong-read request cannot be satisfied by the limited provider. Gates 1–5 execute seven native acceptance tests with zero skips. Process lifecycle remains the independent `LocalProcessLifecycleCapability` contract, with the W3 runner Job and existing Job Object/ConPTY paths reporting `STRONG_DESCENDANT_OWNERSHIP`. W2 provisions real `NeuroSandboxOffline`/`NeuroSandboxOnline` local users and persists their SIDs and actual credentials in a DPAPI machine-scope record protected by exact NTFS deny ACEs; the installation-scoped synthetic SID is only a write-only restricted-token principal and is never a read or firewall identity. Native ACL reconciliation uses `SetEntriesInAclW` for canonical deny-before-allow ordering, preserves unrelated controller ACEs, and repairs exact read/primary-user-write/restricting-write/read-only-deny/sensitive-deny tuples. The installation owns a persistent Offline Firewall block scoped only to the real Offline SID; Online identity use does not remove it, and only explicit cleanup removes the rule. Setup reports `READY`/`NEEDS_SETUP`/`NEEDS_REPAIR`/`UNSUPPORTED` and retains an administrator-only setup boundary with non-admin runtime facts. The W5 workload matrix in CI run `32374860136` passes all 20 rows and all HOST/W3/W4 cells without changing this contract. See [ADR 0113](adr/0113-windows-native-restricted-token-sandbox-architecture.md), [ADR 0114](adr/0114-windows-native-sandbox-runtime.md), and [ADR 0116](adr/0116-windows-developer-workload-compatibility.md) |
| Local-process lifecycle capability contract | M3 | compatible | `LocalProcessLifecycleCapability` explicitly separates `STRONG_DESCENDANT_OWNERSHIP` from `PROCESS_GROUP_BEST_EFFORT`; the single satisfaction helper allows strong adapters to satisfy best-effort requests and makes best-effort adapters fail closed before child creation for strong requirements. Linux enabled Bubblewrap and Windows Job Object/ConPTY paths report strong ownership; plain POSIX and macOS Seatbelt report best effort. Filesystem/network authority remains independent. See [ADR 0110](adr/0110-cross-platform-lifecycle-capability-contract.md) |
| ACP stdio/WebSocket | M4 | partial | Official `agent-client-protocol==0.11.0` stdio SDK plus the bounded WebSocket newline-JSON bridge implement protocol-version negotiation, saved client capabilities/info, stable workspace-bound ACP IDs with durable lazy ACP-to-SQLite-ID aliases, session create/list/load/resume/fork/delete/prompt/cancel/close, bounded/redacted replay, fail-closed permissions, session-owned MCP tools, bounded additional directories, capability-gated client text-file reads plus exact replacements, a capability-gated client terminal, bounded inline-image/audio prompts, embedded text and binary prompt blocks, and private artifact/subagent/lifecycle/MCP/compaction extensions. `loadSession: true`, list/delete/fork/resume/close, and MCP `http: true`/`sse: true` capabilities are advertised. When advertised by the client, `read_file` delegates to `fs/read_text_file`; `search_replace` is exposed only when both text read/write capabilities exist and preserves sandbox, workspace, instruction, and permission gates. With `terminal: true` and only when sandbox is `off`, `terminal_exec` sends one direct executable plus arguments through create/wait/output/release; the standard background terminal lifecycle uses opaque task IDs. Image blocks are base64-validated against a raster MIME allowlist and preserve ordered native provider context; audio and binary blocks are bounded and preserved in model context, while unsupported providers receive placeholders. Binary multimedia history replay and interactive client terminal input/resize/PTY framing remain unsupported. Bounded stdio, Streamable HTTP, and legacy SSE servers are accepted; ACP-transport MCP servers remain unsupported. Canonical `neuro_code.interfaces.acp.agent` owns the protocol agent and delegates transport, content, event projection, client I/O, stateless MCP declaration conversion, and per-session state to the explicit ACP submodules. The former root `neuro_code.acp` implementation has been removed. `ConversationBinding.close()` remains the binding cleanup authority and `SessionTurnService`/`ConversationRunner` remain turn/recovery authorities. Wire behavior is unchanged. See [ADR 0146](adr/0146-acp-update-and-event-projection-boundary.md), [ADR 0147](adr/0147-acp-client-io-adapter-boundary.md), [ADR 0148](adr/0148-acp-mcp-configuration-boundary.md), [ADR 0150](adr/0150-acp-session-runtime-ownership-boundary.md), [ADR 0151](adr/0151-acp-transport-boundary.md), and [ADR 0153](adr/0153-architecture-completion.md). |
| MCP servers | M4 | partial | Official `mcp>=1.28.1,<2` `ClientSession`, schemas, negotiation, JSON-RPC dispatch, tool listing/calls, resources, resource templates, prompts, sampling callbacks, elicitation callbacks, and bounded dynamic refresh serve ACP-provided stdio, Streamable HTTP, and legacy SSE servers. Stateless ACP MCP declaration conversion is canonically owned by `neuro_code.interfaces.acp.mcp_config`; it performs zero I/O, preserves protected-environment rejection and the existing bounded configuration/error contract, and returns the existing application configs. Stdio retains the newline bridge, POSIX process-group, and atomic Windows Job guarantees. Remote endpoints require validated HTTP/HTTPS URLs without embedded credentials or fragments, bounded non-reserved headers, TLS verification, no environment proxy inheritance or redirects, and a 1 MiB response cap. Limits, protected environment rejection, explicit-value redaction, `_meta` omission, collisions, metadata-only ResourceLinks, ASK-over-bypass approval, and idempotent cleanup apply to every transport. Resource/prompt reads and sampling/elicitation callbacks use private bounded ACP extensions, and bounded dynamic tool-list refresh is implemented through the same session-owned path; these are bounded projections, not generic MCP parity. Persistent MCP configuration and ACP-transport MCP server declarations remain unsupported. The ACP stdio/WebSocket server transport is covered separately above. See [ADR 0148](adr/0148-acp-mcp-configuration-boundary.md). |
| Skills and AGENTS.md | M4 | partial | Bounded exact-name `AGENTS.md` inheritance is injected as transient tagged User context, refreshed from a binding-local target before each model step; `search_replace` compares against the last injected path/content snapshot and aborts on unseen changes. Read-only skill discovery scans nested LOCAL roots, every REPO ancestor through the git root, and USER home with deterministic `LOCAL > REPO > USER` shadowing, hard traversal/entry/file/byte/catalog limits, all links/reparse points rejected, and body-sensitive fingerprints. The bounded `skill` tool validates the discovery fingerprint, strips frontmatter, safely samples bundled file names, and supports bounded `$ARGUMENTS`, indexed arguments, and `${SKILL_DIR}` substitution. Alternate instruction names/rules, `.cursor` and vendor-default compatibility, conditional skills, remote/server/bundled skills, hooks/plugins, and subagent integration remain pending |
| Hooks and plugins | M4 | planned | Hooks and plugin marketplace crates |
| Subagents and plan mode | M4 | partial | Durable structured per-session plans are model-managed through `update_plan`, survive resume, inherit on fork, and are viewable in the TUI. Local plan comments, execution handoff, queueing, task claiming, the explicit CLI/TUI/ACP read-only subagent surfaces, lifecycle controls, and parent/child projections are implemented. Every production child-runtime path now receives the actual parent binding capability manifest: CLI opens a parent binding, TUI reads the active binding, and ACP requires an active parent binding. The explicit read-only path treats its fixed tool names as a request and resolves `parent ∩ requested ∩ composition-owned global policy` before binding construction; binding manifests and runtime fingerprints are checked. The bounded application scheduler provides fresh child runtimes, max-parallel limits, retry limits, timeout limits, explicit recursive/depth gates, tool allowlists, read-only/write capability checks, ordered results, and cleanup for every attempt. The arbitrary legacy executor seam is test/internal-only; relationship resume/fork/delete do not recreate runtimes. This proves only `child capability <= actual parent capability`, not the complete permission/workspace/sandbox/MCP/agent security system. Parent-context reuse and unbounded automatic orchestration remain outside this row. Explicit CLI/TUI/ACP subagent entrypoints remain read-only. Separately, the internal bounded Writable Subagent path exposes only `search_replace`/`apply_patch` as write tools when actual parent capability, requested worker capability, composition-owned global policy, managed worktree, sandbox/workspace constraints, and the worker policy all permit it; bounded Task DAG and the `BOUNDED_SWARM` branch of Ultracode may use fresh scoped workers through existing factories. This does not make ordinary subagents writable or grant arbitrary Bash, network, or MCP access. See ADR 0131 and ADR 0141 |
| Explicit subagent lifecycle CLI controls | M4 | partial | Stage5CZ adds `neuro subagents ACTION TASK_ID --parent-session SESSION_ID` for explicit `resume`, `fork`, and `delete` actions through the application lifecycle owner. Plain and typed JSON projections contain only bounded lifecycle identifiers; the command does not run a model, replay tools, open forks automatically, or read SQLite directly. |
| Explicit ACP subagent lifecycle extension | M4 | partial | Stage5DA adds private `_neuro-code/session/subagents` with strict external `sessionId`, bounded `taskId`, and `resume`/`fork`/`delete` actions. Resume/fork return external ACP aliases and delete returns only a deleted flag; no model turn, tool replay, recursive/parallel child, write tool, internal ID, or child transcript crosses the wire. |
| Fail-closed ACP subagent lifecycle projection | M4 | partial | Stage5DB validates returned parent/task/action correlation and bounds non-delete external aliases before wire serialization. Invalid injected results fail closed; valid lifecycle responses and all runtime/session behavior remain unchanged. |
| ACP subagent alias reconnect compatibility | M4 | partial | Stage5DC bounds lifecycle alias allocation to four attempts, resolves each alias back to the expected child before serialization, retries collisions or wrong-owner results, and preserves the durable alias across repeated resume requests and ACP client reconnects. The private extension, child execution, schema, and standard ACP capability set remain unchanged. |
| Deterministic context-compaction assessment | M5 | partial | Stage5DD adds the canonical `neuro_code.application.memory.compaction` planner with typed usage, soft/hard thresholds, protected-prefix and recent-suffix boundaries, and an index-only bounded plan. Unknown capacity is explicit `UNAVAILABLE`; no summaries, provider calls, durable items, Runtime integration, or session/UI/wire behavior are changed. |
| Provider-aware context summary request boundary | M5 | partial | Stage5DE adds bounded `ProviderContextWindow` metadata and `ContextSummaryRequest` projection in the canonical memory module. Known capacity clamps the summary budget; unknown capacity, non-actionable plans, and empty candidate ranges fail closed. No provider parameter, model call, prompt, durable item, Runtime integration, or interface behavior is added. |
| Provider-aware redacted summary input | M5 | partial | Stage5DF adds `ContextSummaryInputBuilder` and typed bounded projections. It redacts explicit and common credential shapes before control/UTF-8 bounds, omits tool arguments, reasoning, and preserved provider payloads, and applies an injected local token estimator against the provider window's remaining budget. It does not call a Provider, build a prompt, mutate context, persist compaction items, or change Runtime/interface behavior. |
| Provider-backed bounded context summary generation | M5 | partial | Stage5DH adds `ProviderContextSummaryGenerator` and `ContextSummaryGenerationResult`. It accepts only the redacted bounded input, validates provider/model affinity, builds a temporary prompt, performs exactly one `DISABLED` no-tool request, buffers deltas, prefers `ModelCompleted.response_text`, redacts/bounds the result, and fails closed on missing/empty/multiple completion or remote tool events. It does not enable automatic compaction, retry, persistence, events, or whole-turn atomicity. |
| Memory and compaction | M5 | partial | Stage5DD establishes deterministic assessment; Stage5DE/DF add provider-aware bounded request/input contracts; Stage5DG adds durable compaction items and explicit resume reconstruction; Stage5DH adds one-request strict no-tool summary generation; Stage5DI adds an explicit application persistence service with pre-call source-fingerprint validation and `SessionStore` delegation; Stage5DJ fixes the compatibility boundary: compaction rows stay out of export/import and fork copying, while `finalize_turn` remains atomic only for its own event/items/search/execution-record transaction. Automatic Runtime triggering and bounded events are enabled only under ADR 0107's controlled, persisted, known-capacity conditions; Provider generation and compaction persistence remain separate transactions. |
| Explicit context-compaction trigger | M5 | partial | Stage5DK adds `ContextCompactionTriggerService` as a default-disabled application boundary. `DISABLED` performs a pure assessment; `EXPLICIT` delegates only an actionable plan after the caller supplies stale-source metadata. The trigger does not change Runtime, Provider, SessionStore schema, export/import, events, CLI, TUI, or ACP behavior and does not reuse ordinary turn budgets. See [ADR 0091](adr/0091-explicit-context-compaction-trigger.md). |
| Runtime compaction safe-boundary gate | M5 | partial | Stage5DL adds a typed gate with `BEFORE_MODEL_REQUEST`/`AFTER_TOOL_BATCH` safe points, fail-closed active-operation/cancellation checks, and a separate one-request/no-tool budget contract. It does not integrate `AgentRuntime`, add events, enable automatic triggering, implement timeouts, or claim whole-turn transactionality. See [ADR 0092](adr/0092-runtime-compaction-safe-boundary.md). |
| Enforced Runtime compaction timeout | M5 | partial | Stage5DM adds a finite, enforced wall-clock limit (30 seconds by default, 300 seconds maximum) around an allowed explicit compaction request, covering summary generation and persistence. Timeout raises `ContextCompactionTimeoutError`; Provider errors, storage errors, and cancellation remain unchanged. It does not integrate `AgentRuntime`, enable automatic compaction, or claim cross-operation transactionality. See [ADR 0093](adr/0093-enforced-context-compaction-timeout.md). |
| Runtime compaction failure projection | M5 | partial | Stage5DN adds `classify_context_compaction_failure()` with a bounded timeout projection to recoverable `BUDGET_LIMITED/WALL_TIME_BUDGET` (`finalized=false`) and a `TURN_FINALIZATION` record policy. Cancellation, Provider, and storage failures remain propagation-only; unknown exceptions are unclassified. The projection stores no error details, does not change `AgentRuntime`, emit events, enable automatic compaction, or claim cross-operation atomicity. See [ADR 0094](adr/0094-runtime-compaction-failure-projection.md). |
| Explicit Runtime compaction seam | M5 | partial | Stage5DO adds an optional, default-disabled `AgentRuntime.compaction_runtime_gate` and `trigger_context_compaction()` for a caller-owned complete safe-boundary request. Missing configuration fails closed; delegation does not mutate context, ordinary turn steps, events, or execution records. `AgentRuntime.run()`, Composition, automatic triggering, and production gate wiring remain unchanged. See [ADR 0095](adr/0095-explicit-runtime-compaction-seam.md). |
| Application-owned explicit compaction caller | M5 | partial | Stage5DP assembles one redaction-aware `ContextCompactionRuntimeGate` per `ApplicationComposition` binding and injects it into `AgentRuntime` without enabling automatic triggering. `AgentConversation.trigger_context_compaction()` is the explicit caller, serializes compaction with the existing session turn lock, requires a matching persisted session for `EXPLICIT` requests, and delegates an immutable caller-owned snapshot. Transcript, events, ordinary turn budgets, and `finalize_turn()` transactionality remain unchanged. See [ADR 0096](adr/0096-application-owned-compaction-caller.md). |
| Atomic turn finalization with compaction | M5 | partial | Stage5DQ adds opt-in `SessionStore.finalize_turn_with_compaction()`; SQLite atomically commits the completion event, session items/search projection, optional execution record, and one durable compaction row. Ordinary `finalize_turn()` and `save_compaction_item()` remain unchanged, provider generation is outside the transaction, and no Runtime or automatic compaction caller uses the new boundary yet. See [ADR 0097](adr/0097-atomic-turn-finalization-with-compaction.md). |
| Turn-recorder compaction finalization owner | M5 | partial | Stage5DR lets `TurnEventRecorder.finalize_turn_completion()` opt into the atomic storage method with a prevalidated `DurableCompactionItem`; ordinary calls remain unchanged, invalid input fails before in-memory completion append, and no current Agent loop or automatic trigger supplies the item. Provider generation and failure projection remain caller-owned. See [ADR 0098](adr/0098-turn-recorder-compaction-finalization-owner.md). |
| Typed compaction turn projection | M5 | partial | Stage5DS adds `ContextCompactionTurnProjection`: successful triggers transfer only the persisted validated item; timeout carries a bounded recoverable budget outcome; cancellation, Provider, storage, and unknown failures are not silently consumed. No events, persistence, automatic compaction, or normal-loop behavior are enabled. See [ADR 0099](adr/0099-context-compaction-turn-projection.md). |
| Explicit compaction turn owner | M5 | partial | Stage5DT adds an opt-in `TurnEventRecorder.finalize_turn_from_compaction_projection()` seam. Successful projections require the caller's ordinary outcome and use atomic turn-plus-item storage; timeout consumes only its bounded outcome; no-op and propagation-only projections fail closed. The normal loop and automatic compaction remain unchanged. See [ADR 0100](adr/0100-explicit-compaction-turn-owner.md). |
| Application compaction owner under the turn lock | M5 | partial | Stage5DU adds `AgentConversation.run_context_compaction_with_owner()` and the matching `ConversationRunner` protocol method. It validates the caller-owned immutable request, serializes the gate and owner under the existing session turn lock, consumes only successful or controlled-timeout projections, and fails closed before the owner for no-op projections. Cancellation, Provider, storage, and unknown failures propagate unchanged; the normal loop, automatic compaction, events, and UI remain unchanged. See [ADR 0101](adr/0101-application-compaction-owner-under-turn-lock.md). |
| Context usage snapshots and stale-source request construction | M5 | partial | Stage5DV adds side-effect-free application helpers that use provider-reported input/output usage when available, bounded `ModelContext` estimation otherwise, retain unknown provider capacity as unknown, and compute a stale-source fingerprint only for an actionable explicit request. They do not call Provider/storage, mutate context, or enable automatic compaction. See [ADR 0102](adr/0102-context-usage-snapshot-and-stale-source-builder.md). |
| Explicit live-context compaction command | M5 | partial | Stage5DW adds `AgentConversation.run_explicit_context_compaction_with_owner()`. It builds the live guided context under the existing session lock, delegates usage/stale-source construction to the configured Runtime gate, allocates bounded identity/time metadata, and reuses the typed owner projection. It requires a persisted session and remains explicit: no normal-loop integration, automatic trigger, event, transcript mutation, or shared Provider/storage transaction. See [ADR 0103](adr/0103-explicit-live-context-compaction-command.md). |
| Explicit compaction command projection | M5 | partial | Stage5DX adds `ContextCompactionCommandResult` and bounded CLI/ACP serializers. `completed`, `not_needed`, and controlled `budget_limited` timeout results expose only opaque IDs, counts, and summary-token metadata; Provider, cancellation, storage, and unknown failures remain exceptions. No command, event, normal-loop integration, or automatic trigger is enabled. See [ADR 0104](adr/0104-explicit-compaction-command-projection.md). |
| Unified ordinary execution budget and replan guidance | M3/M5 | partial | Formal CLI/TUI/ACP entrypoints default to the application-owned `normal` 48/48/192 model/tool-round/tool-call profile, while `deep` selects 96/96/384. Legacy `--max-steps N` now scales all count-based ordinary limits to N/N/4N; Finalizer attempts remain independent. In controlled mode, batch-ending `REPLAN` decisions inject non-persistent synthetic guidance until new progress, and every request receives provider-neutral batch-first guidance. Existing sequential tool execution and stuck detectors remain unchanged. See [ADR 0105](adr/0105-unified-execution-budget-and-replan-guidance.md). |
| Bounded long-task Runtime | M3/M5 | partial | Controlled turns now expose typed budget pressure to the model and bounded interface events, automatically compact at safe boundaries when a persisted session has a known Provider context window, and emit bounded progress segment checkpoints without resetting the global execution budget. The standard TUI deliberately omits raw execution counters. Synthetic budget/checkpoint guidance and compaction requests stay out of the canonical transcript. Compaction Provider generation and persistence are separate operations, and segment checkpoints are audit markers rather than crash-recovery or workspace rollback checkpoints. Bounded Ultracode delegation is a separate entry and does not change this compaction contract. See [ADR 0107](adr/0107-bounded-long-task-runtime.md). |
| Durable turn crash / INDETERMINATE recovery | M5 | partial | Schema 14 introduced the canonical `session_turn_attempts` recovery index; the schema 15-to-16 writable-lease retention migration, schema-17 bounded Parent Relay table, schema-18 durable Task DAG tables, schema-19 durable Leader attempt/decision projections, schema-20 bounded predecessor-result relay table, schema-21 Task DAG recovery-claim fence, schema-22 bounded DAG capacity/scoped lease policy, schema-23 per-node execution-owner identity, schema-24 parallel-aware Leader decision projection, schema-25 bounded model-planning attempt/proposal projections, schema-26 bounded DAG replan attempt/proposal projections, schema-27 bounded Agent Swarm run projection, schema-28 bounded Ultracode delegation projection, schema-29 bounded Result Adoption projections, schema-30 nullable MAIN_MAX verification snapshot columns, schema-31 durable Working Set rows, schema-32 durable active-context generation marker, and schema-33 canonical generation boundaries followed; the SQLite session event store row records the current revision. Request/output/tool write-ahead markers, atomic turn finalization, plan-task ownership, bounded inspect/retry rules, and never-auto-replayed `INDETERMINATE` behavior remain implemented as documented. Background wake input, mid-turn continuation, tool compensation, workspace rollback, and child-process reconciliation remain unsupported. See [ADR 0127](adr/0127-durable-turn-crash-recovery.md). |
| Read-only LSP semantic layer | M5 | partial | Implemented provider-neutral `lsp` tool for definition, references, hover, document/workspace symbols, diagnostics, status, and bounded restart. It uses explicit `[lsp.servers.<name>]` argv profiles, Content-Length JSON-RPC over the canonical local-process port, bounded document synchronization, negotiated UTF-8/UTF-16/UTF-32 positions, workspace/permission filtering, and a real stdio fake-server conformance suite. Rename, format, code actions, and workspace edits remain unsupported. Explicit writable-worker composition is covered separately by ADR 0132. See [ADR 0128](adr/0128-read-only-lsp-vertical-slice.md) |
| Managed Git worktree capability | M5 | partial | Application-owned local lifecycle now supports typed repository identity, exact base-commit creation in detached or `neuro/worktree/<id>` branch mode, durable ownership in an independent `worktrees.db`, bounded status, explicit reconciliation after process death, canonical workspace binding with no inherited additional roots, and safe non-force removal. Git 2.40.0 or newer is required because filter preflight uses `git check-attr --source=<tree-ish>`; older Git is rejected at initialization. Dirty, locked, unmanaged, missing, and mismatched worktrees fail closed. Checkpoint/rollback is a separate internal capability. The explicit standalone writable workspace slice is specified separately by ADR 0131; bounded Task DAG workers use distinct managed Worktrees, while integration, branch deletion, and model-facing Git tools remain absent. See [ADR 0129](adr/0129-managed-git-worktree-capability.md) |
| Managed workspace checkpoint / rollback | M5 | partial | Internal application service now captures an immutable, identity-bound, bounded source projection for a READY Neuro Code-owned managed worktree: exact Git index bytes, tracked changes/deletions/modes/binary/symlinks, and non-ignored untracked files. Separate `checkpoints.db` and state-owned content-addressed artifacts use CAPTURING/READY publication, SHA-256 integrity, insert-only/CAS records, exact-leaf rollback, ignored-file preservation, HEAD match, durable rollback attempts, Git worktree locking, final fingerprint verification, and crash reconciliation. Real child-process death after partial file effects or index restoration converges through normal reconciliation; active artifact corruption becomes durable `INDETERMINATE` without restore or owned-lock release; real rollback/rollback and rollback/remove races are fail-closed. Automatic policy, model/TUI/ACP tools, history rewind, commit/patch/merge integration, and source-checkout rollback remain absent. Git 2.40.0+ is inherited from the Worktree boundary. See [ADR 0130](adr/0130-managed-workspace-checkpoint-rollback.md) |
| Serialized managed writable-subagent workspace | M5 | partial | An explicit standalone service derives a typed grant only from the parent's exact committed HEAD, a Neuro-owned managed branch worktree, and a READY baseline checkpoint. It creates a fresh child session/binding rooted only at that worktree and exposes bounded read tools plus `search_replace`/`apply_patch`; optional read-only `lsp` requires the actual-parent/global/bounded-worker intersection. Bash, terminal, background, MCP, network, Git/worktree/checkpoint/rollback, and recursive subagent authority remain absent. Durable ALLOCATING/WORKTREE_READY/BASELINE_READY/ACTIVE/PRESERVED/ORPHANED/FAILED leases use insert-only/CAS and reconciliation; child changes are preserved with no automatic rollback, removal, merge, commit, copy-back, or cleanup. The standalone service is not a public writable `/subagent` surface: explicit CLI/TUI/ACP subagent entrypoints remain read-only. Bounded Task DAG creates fresh scoped Writable services through its typed factory, and the automatic Ultracode `BOUNDED_SWARM` branch reaches those workers through the existing Swarm, Leader, and Task DAG composition. The Ultracode router itself adds no writable authority; each lower worker remains conditional on the actual parent/request/global capability intersection, managed worktree, and sandbox/workspace constraints. Unbounded parallel writable workers, integration, branch deletion, and model-facing Git tools remain unsupported. See [ADR 0131](adr/0131-managed-writable-subagent-workspace.md) |
| Worker-scoped Worktree + LSP runtime integration | M5 | partial | The standalone writable runtime now derives `WorktreeWorkspaceBinding` from the managed handle and requires exact equality across child binding/capability/worktree/LSP roots with no additional roots. Parent and workers never share manager/client/route/document state; real write-tool changes are resynchronized before semantic reads. LSP uses the child `LocalProcessSandbox`, stays read-only, filters outside URIs, rejects `workspace/applyEdit`, and closes process/cache on success, failure, cancellation, or timeout while preserving worktree/checkpoint/lease evidence. Bounded Task DAG workers retain one independent LSP manager/process/root per worker; integration/merge, cleanup, UI exposure, and writable LSP mutation remain absent. See [ADR 0132](adr/0132-worker-scoped-lsp-runtime-integration.md) |
| Bounded Parent Context Relay | M5 | partial | Each explicit writable worker now receives one deterministic immutable snapshot derived only from the actual parent binding's durable session. It selects redacted genuine USER/visible ASSISTANT plain text under 10-item, 4-KiB-per-item, 24-KiB-projected, and 32-KiB-rendered limits; structurally excludes reasoning, tool calls/arguments/results, media, system, and synthetic runtime context; binds exact worker authority identities without expanding them; stores one insert-only integrity-verified schema-17 row before runtime/model execution, retained by the current schema; and injects one byte-stable synthetic relay per request without writing it to child history. Compaction-summary reuse, live/shared context, unbounded automatic delegation, and other richer result relays remain absent. Bounded Task DAG workers receive separate relays and do not share mutable parent context. See [ADR 0133](adr/0133-bounded-parent-context-relay.md) |
| Bounded durable parallel Task DAG | M5 | partial | ADR 0134 adds an explicit static typed DAG with at most 8 nodes, 16 edges, 4 dependencies per node, deterministic ordinal/node-ID ready selection, and only `WRITABLE_SUBAGENT` nodes. The actual `ConversationBinding` supplies parent identity; existing `SessionTask`, Writable Subagent, Worktree, Checkpoint, Parent Relay, and worker-scoped read-only LSP owners are reused. Immutable `max_parallel` defaults to 1 and is bounded at 4. Schema 18 definitions and node projections, schema 20 predecessor-result relays, schema 21 recovery claims, schema 22 capacity/scoped lease policy, schema 23 per-node execution-owner identity, and schema 24 Leader decision projection are retained with graph/node generation CAS. Durable `RUNNING` node rows are the canonical active set; legacy `active_node_id` is only a compatibility projection. Each claimed node uses a fresh Writable service from a typed factory, preserving per-worker locks and distinct worktree/session/lease/checkpoint/Parent Relay/LSP ownership. Dependencies remain control-only for scheduling; failed descendants become `SKIPPED`, independent branches continue, and no automatic retry or rerun occurs. Fan-out relays are target-generation exact and fan-in waits for all declared direct predecessors. Live execution owners and schema-21 recovery claims protect cross-process startup/recovery windows; post-allocation uncertainty remains `INDETERMINATE` and is never auto-rerun. Real `spawn` acceptance covers provider overlap, K=2 capacity races, distinct managed resources, same-snapshot two-controller races, and per-node no-rerun behavior. Model-generated decomposition, dynamic/unbounded dataflow, automatic delegation, merge, rollback, cleanup, and UI/ACP exposure remain outside this slice; the internal bounded Swarm composition is covered separately by ADR 0140 and the bounded Ultracode entry by ADR 0141. See [ADR 0134](adr/0134-durable-serialized-task-dag.md) and [ADR 0136](adr/0136-bounded-task-dag-predecessor-result-relay.md) |
| Bounded DAG predecessor result relay | M5 | partial | ADR 0136 implements one deterministic relay for each dependent node generation: only direct, declaration-ordered, `COMPLETED` predecessor projections are included, with 4-KiB per-result, 16-KiB aggregate, and 24-KiB rendered bounds. Schema-20 rows bind the exact DAG/target definition, generation, worker task/session, preserved writable lease, worktree, baseline checkpoint, and Parent Relay; they are insert-only, integrity-checked, idempotent for an exact duplicate, and fail closed on missing, stale, tampered, or conflicting evidence. Schema 21 adds the separate recovery-claim fence bound to the same node/parent-task/relay identity. A read-only `SAFE_NOT_STARTED` boundary reuses an already READY relay only when matching task/lease/link evidence is absent and no live recovery owner exists; `RECOVERY_OWNED` protects live or unproven owners, including the lease-only partial window, while a dead owner before lease allocation can be taken over by exact CAS. The loser cannot start a provider or write `FAILED`/`INDETERMINATE`; post-allocation ambiguity remains fail-closed with no auto-rerun. The message is a separate synthetic context channel and cannot grant tools, paths, capabilities, workspace state, or authority. Real process-death acceptance proves relay identity is unchanged, two controllers share exclusive ownership, B allocates exactly once after safe continuation, and post-allocation ownership is not replayed. Dynamic/unbounded dataflow scheduling, merge, retry, and UI/ACP exposure remain absent. See [ADR 0136](adr/0136-bounded-task-dag-predecessor-result-relay.md) |
| Bounded serialized Leader controller | M5 | partial | ADR 0135 adds one explicit Leader over one pre-created Task DAG as the serialized compatibility path. It reconciles the DAG, reviews a bounded redacted deterministic evidence envelope, and uses a zero-tool Leader model to publish typed `SELECT_NODE` or terminal-only `FINALIZE` decisions. Exact DAG generation, definition/evidence fingerprints, Leader session, turn, owner, and durable attempt identity bind every provider request. An expired `CLAIMED` takeover atomically rebinds the fresh session/turn only when no output, decision, or matching old-session turn evidence exists; a durable `PROVIDER_FENCED` CAS prevents a still-live stale owner from calling the provider, while post-fence recovery fails closed. Committed output and decisions retain historical L1 provenance and are reused by fresh composition services without provider replay. The Leader invokes only the existing Task DAG one-step seam, so `WRITABLE_SUBAGENT` remains the worker authority and the DAG remains the dependency/serial legality authority. Dynamic/unbounded dataflow scheduling, merge, rollback, and UI/ACP exposure remain outside this slice; bounded Swarm composition is covered separately by ADR 0140, and Ultracode entry is covered by ADR 0141 rather than invoked here. Bounded parallel-aware waves are specified by ADR 0137. See [ADR 0135](adr/0135-bounded-serialized-leader-controller.md) |
| Parallel-aware Leader / bounded wave scheduling | M5 | partial | ADR 0137 extends the zero-tool Leader with typed `SELECT_NODES` while preserving `SELECT_NODE`/`max_parallel=1` compatibility. Selection is non-empty, unique, canonical `(ordinal,node_id)` order, bounded by immutable `max_parallel` and current durable free capacity; non-canonical, stale, overflow, unknown, and running-node `FINALIZE` decisions fail closed. Evidence includes parent session, graph/node generations, running IDs, available capacity, READY IDs, dependencies, bounded redacted outcomes, and explicit completed/failed/cancelled/skipped/indeterminate buckets. The Task DAG remains the capacity and execution authority: SQLite graph/node generation CAS claims only selected nodes, never substitutes an unselected READY node, and runs independent Writable services in a structured wave. Schema 24 migrates populated schema-23 Leader rows with parent/selection/generation projections. Durable decision reuse, partial-claim recovery, spawned-process death, provider no-replay, and concurrent-controller fencing are covered by focused and production-shaped tests. Dynamic graph, replan, retry, merge, rollback, cleanup, and public UI/ACP remain outside this slice; bounded Swarm composition is covered separately by ADR 0140, and the Ultracode entry is covered by ADR 0141 rather than this wave service. See [ADR 0137](adr/0137-parallel-aware-leader-bounded-wave-scheduling.md) |
| Bounded model-generated DAG planning | M5 | partial | ADR 0138 adds one explicit internal zero-tool Planner that converts one actual-parent objective into one bounded immutable Task DAG proposal. The Planner binding has no local/provider-hosted tools, filesystem, Bash, terminal, network, MCP, LSP, Worktree, Checkpoint, worker, or background authority; it receives only a bounded deterministic redacted USER/visible-ASSISTANT context envelope. Strict JSON admits only `nodes`, `max_parallel`, and bounded `reason`; nodes contain only `id`, `prompt`, and `depends_on`, use declaration-order/canonical dependency order, and retain the frozen Task DAG node, edge, dependency, prompt, and parallelism bounds. TaskDagApplicationService remains the validation/publication authority. Schema 25 added insert-only planning attempts/proposals with actual parent/planner session and turn identity, objective/context/proposal fingerprints, and a preallocated intended DAG ID; the current schema retains them. Claim/fence/model-commit/proposal/DAG publication lifecycle and spawned-process crash recovery never replay observable provider output, change an immutable proposal, or create a second DAG; live/unproven owners are not stolen. The production-shaped acceptance runs Planner → real parallel-aware Leader → real isolated Writable workers and verifies A→B/C→D, B/C overlap, D ordering, distinct managed worktrees, and an unchanged dirty parent checkout. Bounded failed-DAG revision/replan is implemented as the separate internal slice in [ADR 0139](adr/0139-bounded-dag-revision-replan.md); the internal bounded Swarm composition is implemented separately in [ADR 0140](adr/0140-bounded-agent-swarm-durable-orchestration-run.md), and the Ultracode entry in ADR 0141. Recursive replan, retry, dynamic/unbounded dataflow, automatic delegation within this planning slice, merge/copy-back, rollback, cleanup, public UI/ACP, and recursive Ultracode remain unsupported. See [ADR 0138](adr/0138-bounded-model-generated-dag-planning.md) |
| Bounded DAG revision / replan | M5 | partial | ADR 0139 adds one explicit internal revision of one quiescent `FAILED` source DAG into one immutable successor DAG. The source is never mutated and must have no `RUNNING` or unresolved `INDETERMINATE` nodes; `MAX_DAG_REPLAN_DEPTH=1` permits one successor only. A deterministic redacted evidence envelope carries only exact source identity/generation, canonical node state/dependencies, bounded completed-result projections, typed failure summaries, and safe metadata; transcript, tool data, logs, environment, secrets, workspace bytes, checkpoints, diffs, paths, and authority instructions are excluded. The dedicated Planner binding has zero tools, filesystem, Bash, terminal, network, MCP, LSP, Worktree, Checkpoint, worker, and background authority with `max_steps=1`, and returns only the existing typed `ModelDagProposal`. Schema 26 stores insert-only attempt/proposal projections with exact source/evidence/proposal/successor identity; the current schema retains them. Durable claim/fence/model-commit/proposal/successor/completion lifecycle, source snapshot fencing, exact duplicate recovery, one-winner races, spawned-process crash recovery, and no-provider-replay semantics are covered by fixture-provider tests; the provider-turn-evidence boundary becomes explicit `INDETERMINATE`. Publication and later Leader/Writable execution remain owned by existing services. Recursive/multi-level replan, automatic retry, source resurrection, merge/copy-back, rollback, cleanup, public CLI/TUI/ACP orchestration, Ultracode entry invocation, and live/paid provider validation remain unsupported in this replan slice; bounded Swarm composition is covered separately by [ADR 0140](adr/0140-bounded-agent-swarm-durable-orchestration-run.md). See [ADR 0139](adr/0139-bounded-dag-revision-replan.md) |
| Bounded Agent Swarm / durable orchestration run | M5 | partial | ADR 0140 adds one internal bounded composition over the existing model-generated Planner, parallel-aware Leader, Task DAG, Writable Subagent, Parent Context Relay, predecessor-result Relay, Worktree, Checkpoint, worker-scoped LSP, and bounded Replan services. It owns only one parent-bound Swarm identity, durable lifecycle, exact DAG lineage, bounded terminal response, and one-controller ownership fence. The production-shaped normal path is Planner → A/B,C/D DAG → real Leader `SELECT_NODES` with B/C overlap → distinct Writable workers/worktrees/checkpoints/worker-scoped LSP → durable predecessor-result fan-in → D after B+C → Leader `FINALIZE` → durable result. The failure path accepts only one quiescent terminal `FAILED` source without `INDETERMINATE`, creates one immutable successor at `MAX_DAG_REPLAN_DEPTH=1` through the existing Replan service, and never retries, resurrects source nodes, or creates a second successor. Schema 27 adds insert-once `orchestration_swarm_runs` with parent/planning/DAG lineage, lifecycle, redacted result, foreign-key `RESTRICT`, process-liveness ownership, and generation CAS. A representative `multiprocessing`-spawn matrix proves four fresh-process handoffs—Planner state before Swarm `PLANNED`, terminal lower DAG before `FINALIZING`, Replan successor before current-DAG switch with an immutable source, and `FINALIZING` result before `COMPLETED`—with exact identity/resource counts and no provider replay; a composition-level lower `INDETERMINATE` result is terminal and not replanned. This is representative bounded evidence, not arbitrary kill-point or live/paid-provider coverage. Fresh controllers reuse durable terminal results; live-stale losers perform no provider, DAG, worker, or result mutation, and provider-turn uncertainty/cancellation is terminal `INDETERMINATE`. It is internal only: recursive Swarm, unbounded agents/queues, generic retry, shared worktrees, merge/copy-back/cherry-pick/patch adoption, public CLI/TUI/ACP orchestration, remote/cloud execution, marketplace integration, and a new Checkpoint/Rollback implementation remain unsupported. See [ADR 0140](adr/0140-bounded-agent-swarm-durable-orchestration-run.md) |
| Automatic Ultracode delegation / durable branch entry | M5 | partial | ADR 0141 adds the explicit `ULTRACODE` application entry. The current deterministic bounded policy is a fixed marker heuristic for parallel/decomposition, cross-file, and research wording—not a semantic/model classifier—and durably selects exactly one `MAIN_MAX` path through the existing normal ConversationRunner or one `BOUNDED_SWARM` path through the existing Agent Swarm. The interactive TUI binds one dormant delegate and accepts runtime `max` ↔ `ultracode` switching through one long-lived turn service; ordinary efforts never delegate. Schema-28 insert-once identity, exact downstream IDs, parent-visible exactly-once result finalization, provider-wire neutrality, and no fallback remain enforced. The raw fresh-process A–E matrix is direct durable-state evidence; two additional fresh-composition process-death acceptances cover MAIN_MAX after the exact parent result is durable and BOUNDED_SWARM after the existing Swarm is complete, with exact identity, one provider-call/no-replay evidence, and unchanged Planner/Leader/Worker/lease/worktree/checkpoint/DAG resource counts. The stacked automatic result-adoption integration is implemented separately by ADR 0144; ordinary efforts, recursive orchestration, retry, merge/copy-back, remote execution, and public dashboard surfaces remain outside the slice. See [ADR 0141](adr/0141-automatic-ultracode-delegation.md) |
| Bounded durable result adoption core | M5 | partial | ADR 0143 adds an explicit internal application service that consumes only completed Swarm/Task DAG projections, preserved Writable leases, managed READY Worktrees, READY baseline Checkpoints, and the live worker projection to generate an application-owned immutable `ResultAdoptionPlan`. Exact parent `ConversationBinding` session/root/repository/HEAD and source identities are rechecked; worker response text, Leader/Relay text, `git diff`, model paths, and caller manifests are not authority. A conservative three-way plan supports regular-file `CREATE`/`UPDATE`/`DELETE`, preserves unrelated parent dirty files, rejects parent same-path conflicts before `APPLYING`, rejects overlap between workers, and fails closed on links, special files, mode-only changes, protected state, credential/key paths, and outside-root targets. Schema 29 adds insert-only adoption and per-target CAS projections with `CLAIMED -> VERIFIED -> APPLYING -> VERIFYING -> COMPLETED` plus `CONFLICT`/`FAILED`/`INDETERMINATE`; the current schema retains them. Expected/desired image recovery never overwrites a third image, and completed adoption is idempotent. Parent writes reuse the canonical permission/workspace/sandbox/exact-file pipeline; `DELETE` receives no broad `WORKSPACE_EDITS` candidate. Production-shaped temporary-Git tests cover A/B/C/D fresh-process recovery, A/C adoption, B/U/HEAD preservation, worker evidence preservation, third-party-byte preservation on `INDETERMINATE`, completed re-entry with zero new mutation/resource rows, explicit DELETE approval without a broad candidate, conflicts, overlap, and schema 28-to-29 migration/idempotent initialization. Automatic Ultracode wiring is provided only by the separate ADR 0144 composition slice; model merge, cleanup, rollback, commit/push, remote execution, TUI/ACP exposure, and a general merge/copy-back engine remain outside this core slice. See [ADR 0143](adr/0143-bounded-durable-result-adoption.md) |
| Automatic Ultracode result adoption integration | M5 | partial | ADR 0144 wires only explicit `ULTRACODE` user turns into the existing bounded Swarm and internal Result Adoption service. `MAIN_MAX` performs zero adoption activity. Legacy `BOUNDED_SWARM` passes the exact canonical terminal `AgentSwarmResult` and actual parent `ConversationBinding` to Result Adoption, then commits the bounded external result. Structured `BOUNDED_SWARM` uses the same lower path but starts the real parent AgentRuntime after adoption; the parent owns verification, mutation freshness, finalization, and the committed response, while adoption conflict/indeterminate uses a parent-owned deterministic fallback. Existing `ULTRACODE_DELEGATION_PROGRESS` exposes safe adoption stages. Fresh-process recovery proves no completed lower-work replay, no duplicate parent attempt or assistant item, preserved Worktree/Checkpoint/lease resources, and exact committed-parent replay. Semantic merge, cleanup, rollback, commit/push, remote execution, persistent permission grants, and public UI/ACP result-adoption controls remain unsupported. See [ADR 0144](adr/0144-automatic-ultracode-result-adoption-integration.md) and [ADR 0157](adr/0157-ultracode-bounded-swarm-parent-verification.md) |
| Scoped session permission grants / approval-fatigue reduction | M5 | partial | ADR 0142 preserves exact-action `ALLOW_SESSION` and adds memory-only, typed runtime candidates for ordinary primary-workspace `search_replace`/`apply_patch` edits and conservative `test`, `static_check`, and `git_read` command families. Candidates are derived only after canonical target planning or strict Bash classification, are bound to session identity plus canonical workspace root, and never alter authority, sandbox, capability, network, MCP, or policy decisions. Explicit deny/ask, modes, headless requests, protected/non-primary/delete/move targets, high-risk commands, ACP remote input, and model/planner/worker input cannot create a broad grant. Queued equivalent requests re-check the grant; allow-once, denial, and cancellation do not grant waiters. The TUI keeps deny as default focus and shows only bounded root/family metadata; ACP retains exact-action options. No schema or persistent rule change. See [ADR 0142](adr/0142-scoped-session-permission-grants.md) |
| Shell dialect and recoverable command contract | R0.5a | compatible | The public `bash` identity remains unchanged. Shared launcher facts state that POSIX system-shell paths use `/bin/sh` semantics rather than Bash (Bash-specific behavior requires an explicit `bash -c` when available), while Windows uses the trusted `cmd.exe /c`; foreground and managed background paths share the same dialect. Non-zero foreground and background results retain bounded stderr and canonical exit status, remain recoverable in the same model turn, and never trigger an automatic side-effect retry. Generic guidance keeps independent quality checks separate and directs long-running work through managed Bash → `task_output` → `wait_tasks` instead of output-hiding pipelines such as `pytest ... | tail`; permission, sandbox, and process-tree ownership remain unchanged |
| Voice, image, video, web tools | M5 | planned | Provider-backed adapters required |
| Leader, richer relay, Computer Hub | M5 | planned | Local pieces plus external service boundary |

## ACP private extension

The private namespaced `_neuro-code/session/artifacts` extension is now
implemented for bounded, session-associated list/read access to redacted local
tool-output artifacts. It is not advertised as a standard ACP capability;
other bounded private extensions are documented by their owning ADRs, while
methods outside the accepted extension boundaries remain unsupported.
