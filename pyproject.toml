[build-system]
requires = ["hatchling==1.32.3"]
build-backend = "hatchling.build"

[project]
name = "neuro-code"
dynamic = ["version"]
description = "An extensible Python terminal coding agent"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "Neuro Code contributors" }]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Environment :: Console",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Software Development",
]
dependencies = [
  "agent-client-protocol>=0.11.0,<0.12",
  "aiohttp>=3.11,<4",
  "httpx>=0.28,<1",
  "mcp>=1.28.1,<2",
  "pydantic>=2.10,<3",
  "textual>=1.0,<2",
  "websockets>=13,<16",
]

[project.optional-dependencies]
tui = []
socks = ["httpx[socks]>=0.28,<1"]
dev = [
  "mypy>=1.14",
  "packaging>=24",
  "pytest>=8.3",
  "pytest-asyncio>=0.25",
  "pytest-cov>=6.0",
  "ruff>=0.9",
  "textual>=1.0,<2",
  "types-Pygments>=2.19",
]

[project.scripts]
neuro = "neuro_code.bootstrap.entrypoints:main"
neuro-code = "neuro_code.bootstrap.entrypoints:main"

[tool.hatch.build.targets.wheel]
packages = ["src/neuro_code"]

[tool.hatch.build.targets.sdist]
exclude = ["/AGENTS.md", "/.claude", "/.github"]

[tool.hatch.version]
path = "src/neuro_code/_version.py"

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "C4", "DTZ", "LOG", "PIE", "PT", "RUF", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = ["PT009", "PT027"]

[tool.ruff.format]
docstring-code-format = true

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
show_error_codes = true
packages = ["neuro_code"]
mypy_path = "src"

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers -m 'not live'"
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
  "contract: shared adapter contract tests",
  "differential: tests that compare the Rust oracle",
  "live: opt-in tests that require external credentials, network access, and may incur charges",
  "sandbox_integration: real Linux Bubblewrap or macOS Seatbelt security tests",
  "terminal: opt-in tests that require a native terminal or PTY",
]

[tool.coverage.run]
branch = true
source = ["neuro_code"]

[tool.coverage.report]
fail_under = 85
precision = 2
show_missing = true
omit = [
  "src/neuro_code/infrastructure/sandbox/windows_native_local_process.py",
  "src/neuro_code/infrastructure/sandbox/windows_native_runner.py",
]
